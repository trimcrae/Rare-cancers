[s1f] PLACEMENT DECISION: cost_model_red — the fan-out unit-list / cost-model tests came back 'failure' — a broken unit list or cost model must never reach a GPU, so 0 pending unit(s) are held. Nothing was dropped; fix the tests and the next tick places them.
[s1f]   board looked at anyway: 190 offers, best $0.005142/ns on RTX 4090 m12976. Snapshot refreshed, so a stale timestamp on this file can only mean the TICK did not run.
