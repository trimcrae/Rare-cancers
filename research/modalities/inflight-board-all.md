<!-- GENERATED by inflight_board.merge_board(). Do not edit by hand: every lane's tick regenerates
     it from the per-lane fragments. Source of every cell: inflight_board.py. -->
# In-flight board — ALL LANES

Merged 3:32 PM ET Fri Jul 31, 2026. One row per GPU leg, for every lane that can bill.

> ⚠ `inflight-board.md` IS ONE LANE ONLY. Its single writer (`gpu-ternary-fep-vast.yml`
> `task=collect`) rewrites it wholesale from the ternary lane's rows, so it can never carry another
> lane. **This** file is the all-lane board. Each lane writes only its own fragment
> (`inflight-board.d/<lane>.json`, or the file above for the ternary lane) and nothing else, so no
> two writers share a path and no lane can erase another's rows.

> A lane whose fragment is older than 15 min renders STALE rather than vanishing — the line is
> `vast_idle_guard.LOG_SILENCE_MIN`, imported, because a lane's fragment is its heartbeat.

## TERNARY / RUNG 5a-KS — calibrator, triangle and valB replicate legs

_As of 3:25 PM ET Fri Jul 31, 2026 (7 min ago). Written by `ternary_vast_launch.py task=collect`._

```
LEG                  ETA (ET)          % DONE  $/ns                                      STATE     WHY (when not running)
-------------------------------------------------------------------------------------------------------------------------
5aks_d0_to_d ternary 7:57 AM Aug 1      46.7%  RTX 3090 $0.00412/ns · 1.21× basis [bid]  RUNNING   
5aks_d0_to_d ternary 9:02 PM            83.3%  RTX 3090 $0.00554/ns · 1.62× basis [bid]  RUNNING   
5aks_d0_to_d ternary 12:48 PM Aug 1     30.2%  RTX 4090 $0.00533/ns · 1.56× basis [bid]  STARTING  WATCHING — quiet but alive: run.log 2 min old, content changing (last change 0 min ago), GPU idle, no committed advance — consistent with a CPU-bound setup phase
5aks_d0_to_d ternary 7:37 AM Aug 1      32.0%  RTX 4080S $0.00505/ns · 1.48× basis [bid] STARTING  WATCHING — quiet but alive: run.log 2 min old, content changing (last change 0 min ago), GPU idle, no committed advance — consistent with a CPU-bound setup phase
```

## STEP 1 FAN-OUT — the cmpd19 congeneric RBFE map (one unit = complex + solvent legs)

_As of 3:32 PM ET Fri Jul 31, 2026 (0 min ago). Written by `congeneric_fanout_vast.py MONITOR=1`. 18 of 19 unit(s) landed; 1 permanently excluded (rows below are the rest)._

```
IN-FLIGHT BOARD: no GPU legs.
```

## NR-V04 RETROSPECTIVE (Arm E / R1) — 18 endpoint-MD legs

_As of 3:28 PM ET Fri Jul 31, 2026 (4 min ago). Written by `nrv04_vast_launch.py RETRO_COLLECT=1`. 2 of 18 authorized R1 leg(s) landed (rows below are the rest). ⛔ 16 further record(s) exist but are NOT landed legs (mode='smoke', not 'run') — they do not count toward the panel and cannot reach the frozen gate._

```
LEG                ETA (ET)          % DONE  $/ns                                                                                             STATE     WHY (when not running)
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
nr4a1 m1 r0        —                  smoke  —                                                                                                NO HOST   no live host — phase marker md-done 2026-07-31T14:38:24Z; a re-dispatch resumes this leg from its checkpoint. THIS TICK: NOT BOUGHT — it was due for a host this tick and none was rented; see the tick's submit lines for the per-offer refusal (a capacity refusal is not a price hold).
nr4a1 m1 r1        —                      —  — $0.1483/hr (no measured ns/h: endpoint MD, not the 84k-atom RBFE the throughput table benches) STARTING  22 consecutive board polls with no new frame (this host is 4 min old) — ⚠ MOST OF THOSE POLLS PREDATE THIS RENTAL: the counter is on the unit and survives re-placement, and this host is still inside the 15 min cold-start floor, so this is a fresh host carrying an old count, not a wedge; phase marker env-ready 2026-07-31T19:25:01Z, GPU 0.0%
nr4a1 m2 r0        —                  smoke  — $0.2248/hr (no measured ns/h: endpoint MD, not the 84k-atom RBFE the throughput table benches) STARTING  30 consecutive board polls with no new frame (this host is 4 min old) — ⚠ MOST OF THOSE POLLS PREDATE THIS RENTAL: the counter is on the unit and survives re-placement, and this host is still inside the 15 min cold-start floor, so this is a fresh host carrying an old count, not a wedge; phase marker uploaded 2026-07-31T14:29:38Z — ⚠ that marker was written by a PREVIOUS host and survived it; the host holding this unit now started 295 min later, so the marker is not evidence about this rental, GPU utilisation not reported by the host
nr4a1 m2 r1        —                  smoke  — $0.2248/hr (no measured ns/h: endpoint MD, not the 84k-atom RBFE the throughput table benches) STARTING  30 consecutive board polls with no new frame (this host is 1 min old) — ⚠ MOST OF THOSE POLLS PREDATE THIS RENTAL: the counter is on the unit and survives re-placement, and this host is still inside the 15 min cold-start floor, so this is a fresh host carrying an old count, not a wedge; phase marker uploaded 2026-07-31T14:49:50Z — ⚠ that marker was written by a PREVIOUS host and survived it; the host holding this unit now started 277 min later, so the marker is not evidence about this rental, GPU utilisation not reported by the host
nr4a1 m3 r0        —                  20.0%  —                                                                                                NO HOST   no live host — phase marker md-running 2026-07-31T19:01:41Z; a re-dispatch resumes this leg from its checkpoint. THIS TICK: NOT BOUGHT — it was due for a host this tick and none was rented; see the tick's submit lines for the per-offer refusal (a capacity refusal is not a price hold).
nr4a1 m3 r1        —                  smoke  — $0.2289/hr (no measured ns/h: endpoint MD, not the 84k-atom RBFE the throughput table benches) STARTING  30 consecutive board polls with no new frame (this host is 1 min old) — ⚠ MOST OF THOSE POLLS PREDATE THIS RENTAL: the counter is on the unit and survives re-placement, and this host is still inside the 15 min cold-start floor, so this is a fresh host carrying an old count, not a wedge; phase marker uploaded 2026-07-31T14:21:02Z — ⚠ that marker was written by a PREVIOUS host and survived it; the host holding this unit now started 306 min later, so the marker is not evidence about this rental, GPU utilisation not reported by the host
nr4a2 m2 r0        —                      —  — $0.2289/hr (no measured ns/h: endpoint MD, not the 84k-atom RBFE the throughput table benches) STARTING  15 consecutive board polls with no new frame (this host is 1 min old) — ⚠ MOST OF THOSE POLLS PREDATE THIS RENTAL: the counter is on the unit and survives re-placement, and this host is still inside the 15 min cold-start floor, so this is a fresh host carrying an old count, not a wedge; phase marker md-running 2026-07-31T17:55:56Z — ⚠ that marker was written by a PREVIOUS host and survived it; the host holding this unit now started 91 min later, so the marker is not evidence about this rental, GPU utilisation not reported by the host
nr4a2 m2 r1        —                  smoke  — $0.2329/hr (no measured ns/h: endpoint MD, not the 84k-atom RBFE the throughput table benches) STARTING  30 consecutive board polls with no new frame (this host is 1 min old) — ⚠ MOST OF THOSE POLLS PREDATE THIS RENTAL: the counter is on the unit and survives re-placement, and this host is still inside the 15 min cold-start floor, so this is a fresh host carrying an old count, not a wedge; phase marker uploaded 2026-07-31T14:28:02Z — ⚠ that marker was written by a PREVIOUS host and survived it; the host holding this unit now started 299 min later, so the marker is not evidence about this rental, GPU utilisation not reported by the host
nr4a2 m3 r0        —                  smoke  — $0.2329/hr (no measured ns/h: endpoint MD, not the 84k-atom RBFE the throughput table benches) STARTING  30 consecutive board polls with no new frame (this host is 1 min old) — ⚠ MOST OF THOSE POLLS PREDATE THIS RENTAL: the counter is on the unit and survives re-placement, and this host is still inside the 15 min cold-start floor, so this is a fresh host carrying an old count, not a wedge; phase marker md-running 2026-07-31T15:07:57Z — ⚠ that marker was written by a PREVIOUS host and survived it; the host holding this unit now started 260 min later, so the marker is not evidence about this rental, GPU utilisation not reported by the host
nr4a2 m3 r1        —                  smoke  — $0.2370/hr (no measured ns/h: endpoint MD, not the 84k-atom RBFE the throughput table benches) STARTING  30 consecutive board polls with no new frame (this host is 1 min old) — ⚠ MOST OF THOSE POLLS PREDATE THIS RENTAL: the counter is on the unit and survives re-placement, and this host is still inside the 15 min cold-start floor, so this is a fresh host carrying an old count, not a wedge; phase marker uploaded 2026-07-31T14:44:43Z — ⚠ that marker was written by a PREVIOUS host and survived it; the host holding this unit now started 283 min later, so the marker is not evidence about this rental, GPU utilisation not reported by the host
nr4a3 m1 r0        —                  40.0%  — $0.2370/hr (no measured ns/h: endpoint MD, not the 84k-atom RBFE the throughput table benches) STARTING  6 consecutive board polls with no new frame (this host is 1 min old) — ⚠ MOST OF THOSE POLLS PREDATE THIS RENTAL: the counter is on the unit and survives re-placement, and this host is still inside the 15 min cold-start floor, so this is a fresh host carrying an old count, not a wedge; phase marker md-running 2026-07-31T18:39:53Z — ⚠ that marker was written by a PREVIOUS host and survived it; the host holding this unit now started 48 min later, so the marker is not evidence about this rental, GPU utilisation not reported by the host
nr4a3 m1 r1        —                  smoke  — $0.1846/hr (no measured ns/h: endpoint MD, not the 84k-atom RBFE the throughput table benches) STARTING  30 consecutive board polls with no new frame (this host is 0 min old) — ⚠ MOST OF THOSE POLLS PREDATE THIS RENTAL: the counter is on the unit and survives re-placement, and this host is still inside the 15 min cold-start floor, so this is a fresh host carrying an old count, not a wedge; phase marker uploaded 2026-07-31T14:16:33Z — ⚠ that marker was written by a PREVIOUS host and survived it; the host holding this unit now started 311 min later, so the marker is not evidence about this rental, GPU utilisation not reported by the host
nr4a3 m2 r0        —                  smoke  — $0.2411/hr (no measured ns/h: endpoint MD, not the 84k-atom RBFE the throughput table benches) STARTING  30 consecutive board polls with no new frame (this host is 0 min old) — ⚠ MOST OF THOSE POLLS PREDATE THIS RENTAL: the counter is on the unit and survives re-placement, and this host is still inside the 15 min cold-start floor, so this is a fresh host carrying an old count, not a wedge; phase marker md-running 2026-07-31T14:25:27Z — ⚠ that marker was written by a PREVIOUS host and survived it; the host holding this unit now started 302 min later, so the marker is not evidence about this rental, GPU utilisation not reported by the host
nr4a3 m2 r1        —                  smoke  — $0.2207/hr (no measured ns/h: endpoint MD, not the 84k-atom RBFE the throughput table benches) STARTING  30 consecutive board polls with no new frame (this host is 0 min old) — ⚠ MOST OF THOSE POLLS PREDATE THIS RENTAL: the counter is on the unit and survives re-placement, and this host is still inside the 15 min cold-start floor, so this is a fresh host carrying an old count, not a wedge; phase marker md-running 2026-07-31T14:39:30Z — ⚠ that marker was written by a PREVIOUS host and survived it; the host holding this unit now started 289 min later, so the marker is not evidence about this rental, GPU utilisation not reported by the host
nr4a3 m3 r0        —                      —  — $0.2411/hr (no measured ns/h: endpoint MD, not the 84k-atom RBFE the throughput table benches) STARTING  29 consecutive board polls with no new frame (this host is 0 min old) — ⚠ MOST OF THOSE POLLS PREDATE THIS RENTAL: the counter is on the unit and survives re-placement, and this host is still inside the 15 min cold-start floor, so this is a fresh host carrying an old count, not a wedge; phase marker env-ready 2026-07-31T19:26:15Z — ⚠ that marker was written by a PREVIOUS host and survived it; the host holding this unit now started 2 min later, so the marker is not evidence about this rental, GPU utilisation not reported by the host
nr4a3 m3 r1        —                  smoke  — $0.2207/hr (no measured ns/h: endpoint MD, not the 84k-atom RBFE the throughput table benches) STARTING  30 consecutive board polls with no new frame (this host is 2 min old) — ⚠ MOST OF THOSE POLLS PREDATE THIS RENTAL: the counter is on the unit and survives re-placement, and this host is still inside the 15 min cold-start floor, so this is a fresh host carrying an old count, not a wedge; phase marker uploaded 2026-07-31T14:41:18Z — ⚠ that marker was written by a PREVIOUS host and survived it; the host holding this unit now started 284 min later, so the marker is not evidence about this rental, GPU utilisation not reported by the host
```

