{
  "_what": "Declarative watch list for the VAST ternary watchdog (.github/workflows/ternary-vast-watchdog.yml).",
  "_why": "The GCP watchdog (ternary-leg-watchdog.yml) cannot watch a Vast leg: it authenticates by WIF, reads state from GCS, asks 'is a gcp-ternary VM up?', and its only recovery is to re-dispatch the GCP workflow. Pointing a Vast leg at it would produce monitoring that silently watches nothing. Under the 2026-07-25 all-runs-on-Vast directive that gap would apply to every GPU run the program makes from here.",
  "_how": "The workflow runs on a cron. Per enabled entry it asks three questions against S3 and the Vast API: is leg.json already there (DONE), is an instance up AND the committed iteration count HIGHER than last tick (RUNNING), or is it dead/stalled? Only a DIED entry relaunches, resuming from the last committed checkpoint; a stall is an alert, because a relaunch would hang the same way. A per-UTC-day relaunch counter in S3 caps runaway re-dispatch and every outcome is an annotation; any alert also FAILS the job so GitHub's own failure notification fires.",
  "_alive_is_not_advancing": "The Vast-specific correction over the GCP version. A rented instance can sit up with a dead container or an idle GPU and look perfectly healthy, so RUNNING requires the commit store to have ADVANCED, not merely for an instance to exist.",
  "_capacity_refusal": "A start answered with {'success': false, 'error': 'resources_unavailable'} is NOT a preemption and is NOT waited out: destroy, add the machine to the exclusion set, pick another host. That policy lives in ternary_vast_launch.collect + ResourceSpec.exclude_machine_ids and is delegated to, never duplicated.",
  "_editing": "Set enabled=false when a unit is finished or deliberately parked \u2014 do NOT delete the entry, so the history of what was watched stays legible. The watchdog is a no-op on an empty or all-disabled list.",
  "_required_keys_are_enforced": "Every parameter that KEYS THE COMMIT PREFIX or the code the host runs must appear on each enabled entry, because the watchdog relaunches from these values alone. The prefix is <leg_id>_r<seed>[_dir<direction>]_dt<timestep_fs>fs_wu<warmup_timestep_fs>_<mode>, so an entry missing one would make the watchdog 'resume' a DIFFERENT trajectory than the run it is watching. git_branch is required for the same reason and is easy to miss: a schedule only fires from the default branch, so github.ref_name inside the watchdog is 'main' \u2014 without recording the branch a unit was launched from, a relaunch would pull main's code onto a feature branch's checkpoint. Validated by watchdog_validate.py on every pass; an incomplete entry aborts the pass rather than relaunching the wrong thing.",
  "_prefix_keying_params": [
    "unit_id",
    "leg_id",
    "seed",
    "direction",
    "mode",
    "timestep_fs",
    "warmup_timestep_fs",
    "git_branch"
  ],
  "watch": [
    {
      "unit_id": "calib_hi_to_lo__ternary_vhl_r0_dt4.0fs_wu1.0_probe",
      "leg_id": "calib_hi_to_lo__ternary_vhl",
      "seed": 0,
      "direction": "fwd",
      "mode": "probe",
      "timestep_fs": "4.0",
      "warmup_timestep_fs": "1.0",
      "git_branch": "claude/max-effort-2dq11l-4fs",
      "max_relaunches_per_day": 8,
      "enabled": false,
      "_disabled_why": "leg.json is in S3 and the watchdog reported DONE 2026-07-26 02:04Z (probe dG_morph=48.1969622572336, solvent dG_morph=47.798183448497845, NaN=False). Left in the list rather than deleted so the completed unit stays on the record."
    },
    {
      "unit_id": "calib_hi_to_lo__ternary_vhl_r0_dt4.0fs_wu1.0_edge",
      "leg_id": "calib_hi_to_lo__ternary_vhl",
      "seed": 0,
      "direction": "fwd",
      "mode": "edge",
      "timestep_fs": "4.0",
      "warmup_timestep_fs": "1.0",
      "git_branch": "claude/max-effort-2dq11l-4fs",
      "max_relaunches_per_day": 8,
      "enabled": false,
      "_disabled_why": "leg.json is in S3 and the watchdog reported DONE 2026-07-26 11:37 AM ET (dG_morph=47.6131141875807, NaN=False). All four RUNG 2b legs are now landed and reduced. Left in the list rather than deleted so the completed unit stays on the record."
    },
    {
      "unit_id": "calib_hi_to_lo__binary_vhl_r0_dt4.0fs_wu1.0_edge",
      "leg_id": "calib_hi_to_lo__binary_vhl",
      "seed": 0,
      "direction": "fwd",
      "mode": "edge",
      "timestep_fs": "4.0",
      "warmup_timestep_fs": "1.0",
      "git_branch": "claude/max-effort-2dq11l-4fs",
      "max_relaunches_per_day": 8,
      "enabled": false,
      "_disabled_why": "leg.json is in S3 and the watchdog reported DONE 2026-07-26 6:37 AM ET (dG_morph=48.12563207150974, NaN=False). Left in the list rather than deleted so the completed unit stays on the record."
    },
    {
      "unit_id": "calib_hi_to_lo__solvent_r0_dt4.0fs_wu1.0_edge",
      "leg_id": "calib_hi_to_lo__solvent",
      "seed": 0,
      "direction": "fwd",
      "mode": "edge",
      "timestep_fs": "4.0",
      "warmup_timestep_fs": "1.0",
      "git_branch": "claude/max-effort-2dq11l-4fs",
      "max_relaunches_per_day": 8,
      "enabled": false,
      "_disabled_why": "leg.json is in S3 and the watchdog reported DONE 2026-07-26 02:04Z (probe dG_morph=48.1969622572336, solvent dG_morph=47.798183448497845, NaN=False). Left in the list rather than deleted so the completed unit stays on the record."
    },
    {
      "unit_id": "5aks_d0_to_d__ternary_nr4a3_r0_dt4.0fs_wu1.0_5aks_smoke",
      "leg_id": "5aks_d0_to_d__ternary_nr4a3",
      "seed": 0,
      "direction": "fwd",
      "mode": "5aks_smoke",
      "timestep_fs": "4.0",
      "warmup_timestep_fs": "1.0",
      "git_branch": "claude/max-effort-2dq11l",
      "max_relaunches_per_day": 8,
      "enabled": true
    }
  ]
}
