# Tests known to FAIL in the dev sandbox because a scientific dependency is absent.
#
# ⛔ THIS FILE EXISTS BECAUSE A COUNT LET A REAL FAILURE THROUGH (2026-08-08). The pytest gate in
# scripts/preflight.sh tolerated a NUMBER of sandbox failures and asserted they were "all dep-related"
# without checking. A genuine regression -- test_lint_citations.py::test_the_ledger_does_not_anchor_itself,
# broken by a newly committed artifact -- took the count from 48 to 49, sat under the baseline of 50, and
# the gate printed PREFLIGHT OK. That tree was pushed and turned `main` red. CI with full deps reported
# exactly one failure against 7,363 passes, so nothing about the failure was ambiguous; the local gate
# simply could not see it. preflight.sh's own comments had already recorded the limitation in words --
# "This script stores no baseline LIST, only a COUNT, so the excess cannot be identified here" -- which is
# why the fix is a list and not a bigger number.
#
# ⚠ THIS IS NOT AN IGNORE LIST. An ignore list suppresses; this one NAMES what is tolerated, fails the
# build on anything absent from it, and reports entries that have started passing so the list shrinks.
# A test may only be added here after its failure is traced to a missing module IN THIS SANDBOX -- not
# because it is inconvenient.
#
# Regenerate deliberately, in a commit, when the sandbox's installed packages change:
#   python3 -m pytest research/modalities/tests/ -q --continue-on-collection-errors \
#     --ignore=research/modalities/tests/test_ternary_endpoint_align.py \
#   | grep '^FAILED' | sed 's/^FAILED //; s/ - .*//' | sort > research/modalities/tests/sandbox-failure-baseline.txt
#   (then re-add this header)
#
# ⭐ PRUNED 53 -> 14 ON 2026-08-17, AND THE PRUNE IS THE DIRECTION THIS FILE IS BUILT TO WELCOME.
# A deposit session installed numpy, rdkit, boto3, biopython and jsonschema, so 39 entries stopped
# failing. The gate had been REPORTING that for several runs -- "39 baseline entr(y/ies) no longer fail
# -- prune them" -- and a green build that always carries a notice is a notice nobody reads, which is
# how the next real one gets missed. The list is now what the sandbox actually does.
#
# ⛔ THE PRUNE WAS GATED ON A SET COMPARISON, NEVER ON A COUNT (the rule preflight.sh states for the
# dangerous direction applies to this one too, because a prune that quietly drops a REAL failure is
# indistinguishable from a prune that drops fixed ones). Measured on the run that produced this file:
#   14 failed, 7,756 passed, 58 skipped
#   comm -23 <new failures> <old baseline>  ->  EMPTY. Not one failure is new; all 14 are a strict
#   subset of the 53, so nothing was absorbed by the shrink.
#
# ⭐ ALL 14 WERE TRACED TO A MODULE, NOT ASSUMED. From this run's own output:
#   10  ModuleNotFoundError: No module named 'pymbar'  (test_abfe_diagnostics.py)
#    3  ModuleNotFoundError: No module named 'scipy'   (test_slow_cv.py)
#    1  test_abfe_diagnostics.py::test_receptor_dg_bind_matches_combine_legs -- same pymbar gap,
#       reported without a ModuleNotFoundError line because it fails inside a helper that catches it.
# Verified directly: `import pymbar` and `import scipy` both fail here; numpy, rdkit and boto3 now
# all import, which is exactly why the 39 could go.
#
#
# ⭐ PRUNED 14 -> 11 ON 2026-08-19: scipy now imports here, so the three test_slow_cv entries
# stopped failing. The gate had reported it for four consecutive runs -- the same "a notice nobody
# reads" drift the 2026-08-17 prune above was written about, arriving again.
#
# ⛔ GATED ON THE SET COMPARISON, NOT THE COUNT, per the rule stated above. The evidence is
# preflight's own output on this tree (PREFLIGHT_FULL=1, 7,760 passed):
#   "11 failure(s), every one named in the sandbox baseline as dep-related"  -> new failures are a
#   strict SUBSET of the baseline; nothing new was absorbed by the shrink.
#   "3 baseline entr(y/ies) no longer fail"                                  -> and they are named,
#   all three in test_slow_cv.py, matching the scipy group traced in the 2026-08-17 block.
#   14 baseline - 3 no-longer-failing = 11 observed. The arithmetic closes on both sides.
# No separate serial re-run was needed: preflight performs exactly this comparison every build.
# THIS NUMBER SHOULD KEEP FALLING as the sandbox gains packages. It describes a deficient
# environment; it is not a tolerance for broken tests, and every entry is a missing import.
#
# ================================================================================================
# BASELINE IS EMPTY BY MEASUREMENT (2026-08-23), NOT BY TRUNCATION.
#
# A PREFLIGHT_FULL=1 run measured 7,803 modality tests passing, 48 skipped, ZERO failures, and
# reported all eleven remaining entries as "no longer fail -- prune them from
# research/modalities/tests/sandbox-failure-baseline.txt". They were pruned, and each was then
# re-run directly to confirm it passes (test_abfe_diagnostics.py: 21 passed).
#
# ⚠ WHAT AN EMPTY LIST NOW ASSERTS, AND IT IS A STRONGER CLAIM THAN A FULL ONE: this sandbox
# reproduces CI exactly, so EVERY failure is a real failure and none is tolerated. That is the
# correct state, and it is perishable — a dependency can go missing again. When one does, add the
# test id back with the missing dependency named, and DELETE THIS BLOCK, because the claim above
# will no longer be true.
#
# ⛔ NEVER ADD AN ENTRY TO SILENCE A FAILING TEST. An entry means "the dependency is absent here and
# present in CI", and nothing else. test_sandbox_failure_baseline.py asserts this block is present
# whenever the list is empty, so emptiness can never be reached by accident or by truncation.
# ================================================================================================
