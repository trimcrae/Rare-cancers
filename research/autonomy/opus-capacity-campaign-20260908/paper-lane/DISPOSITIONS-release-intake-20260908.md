# Root dispositions — limited release intake, recorded

**2026-09-08, parent, ordinary record. ⛔ No new proposal or approval loop, no auditor, no fresh test
worker, no repeat run.** Both artifacts were re-hashed by the parent and match root's stated bytes
exactly.

## 1 · I1 evidence-shim directory exclusion — ACCEPTED, for that code scope only

`research/manuscripts/aso_archive_manifest.py` — **146,318 B**, sha256
`acebbb17ec0f89e77a9071d3eff56fc1640783ef4f178ded93a096ab1496e0cc`, parent-verified. Root fully read
the actual `78b3f471` → `b7fc3e2c` diff.

**Accepted:** the exact exclusion matches the admitted repair, and it preserves the original
evidence, the eligibility of other modules, and the ambiguity behaviour.

⛔ **This is acceptance of that code scope ONLY. It is NOT a claim that current full closure or the
gates passed.** The archive manifest remained STALE at the time of the fix, and that is unchanged by
this disposition.

## 2 · Source-equivalence figure provenance — ACCEPTED, as a representation only

`research/manuscripts/figures/figure-provenance.json` — **3,654 B**, sha256
`a835a29f22f0278f202d8dc6320c208e3091e056ac95523f81336591a7116a33`, parent-verified. Root fully read
the artifact and its complete diff, and had already **independently verified** the 1,379 leaves, the
only-two-changes result and the read fields.

**Accepted:** as a **truthful source-equivalence representation** — the original draw digest is
retained, the new digest is recorded alongside the two changed leaves and the actual read fields.

⛔ **Accepted as representation, NOT as figure generation and NOT as global figure acceptance.**

⚠ **Still OPEN and not resolved by this acceptance:**
- The **fig4 / `fig_matrix` estimand issue remains OPEN.** ⛔ No redraw, and no fusion-output
  scientific acceptance follows.
- **The four corrective PDFs still await root's actual QA.** They are collected, not accepted.
- **The failed full CI `618` is NOT cleared.** Actual settled-candidate publication gates retain
  their existing requirements in full.

## 3 · Source copies — verification recorded, manifest unchanged

All twelve delivered source copies are now byte-, SHA- and Git-blob-verified against the `216bd`
tree. ⚠ Three abbreviated `original_path` values were resolved with the exact `literature/` prefix
**only**; the **original manifest is unchanged** and no re-write was made to accommodate that
reading.

⛔ **No new source retrieval, and no source-sufficiency claim.** ⛔ **The Huang human original is
still absent** — the r2 manifest documents 403 attempts and is not a delivered Table 1.

## 4 · Frozen papers

**MF1** (`0b965a127`, main 46,172 B / `a9058b6c…`) and **FP** (`f44b75588`) stay **frozen** under
their already-running independent final scientific reviews. ⚠ The continuing collection of MF1's raw
original records is **normal preservation, not an extra review-start gate**, and no framing-permission
wait applies.
