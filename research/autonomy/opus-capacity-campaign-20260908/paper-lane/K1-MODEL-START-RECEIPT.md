# K1 — served-model receipt, parsed from the child transcript

Verified by the parent after the K1 contract commit `ba2462a6`, from
`…/subagents/agent-af7ad52123f8eeccd.jsonl`. Start-time snapshot while running, not a final count.

| item | observed |
|---|---|
| child | `af7ad52123f8eeccd` |
| requested | `claude-opus-5`, **medium** effort |
| route | existing saved first-party subscription; no overage, no credits, no paid fallback |
| task | peer review item 4's unresolved figure half — no marker inside a quadrant captioned empty |

Parsed from the transcript, not from the dispatch parameter or a self-report. Final counts and the
**full lifetime span** (not merely the tool window) will be taken from the JSONL at collection —
self-reported tool-call counts have undercounted **five** times running (E1 18/37, G1 13/21, H1 14/18,
I1 23/33, J1 14/22), and J1's reported span was its tool window rather than its lifetime.

**Live counts:** newly executing research children **1** (K1); helpers **1** (the legacy Bash waiter —
computes nothing, not research). E1, F1, G1, H1, I1, J1 are terminal and collected.

**Retention:** K1 works from `/tmp/claude-0/k1-lane/`, deletes nothing, and is **forbidden from
copying any file into a shared repository path** for a baseline or test — the isolation rule tightened
after J1's temporary shared-manuscript swap.
