---
id: DOC-OPUS-CAMPAIGN-TCIP-PATCH-NOTES
title: "Unapplied parent-owned patches — TCIP F01–F13 narrowing"
level: L4
kind: record
status: live
date: 2026-09-08
last_verified: 2026-09-08
---

# Unapplied parent-owned patches

**These are NOT applied.** The lane did not run `git apply`, `git add`, `git commit` or `git push`.
Each patch is tied to the admitted narrowing of the TCIP main manuscript and SI and changes no
numeric input and no historical result.

## `0001-systems-graph-publications-PUB-TCIP-narrowed.patch`

**Target:** `systems/graph/publications.json`, the `PUB-TCIP` entry's `what_it_would_claim` field.
**Tied to:** the F01–F13 repair batch — the authoritative current claim summary must not certify the
claims the manuscript has withdrawn.
**Baseline:** `systems/graph/publications.json`, 63,825 bytes, SHA256
`11b8a785ac95809d844f46f6427601c67f73f9137a063f6d91b4b93ba0d5bf53`, as of 2026-09-08.
**Verified:** `git apply --check -p1` returned **0** against that baseline. Apply with
`git apply -p1 <patch>` from the repository root.
**Shape:** one hunk, one line — the `what_it_would_claim` string. No other field, entry or file is
touched, and no number in the graph changes.

**What the replacement string does.** It keeps the 2026-08-07 demotion record verbatim (the
zero-occurrence measurements of `NR4A3` and `8XTT`, and the modality-general framing). It then adds a
dated 2026-09-08 narrowing to a toolchain-audit and parameter-sensitivity claim, states the parked
stronger claim, restates the claim itself with the retained counts (583/651, 6,315/5,632,
57,657/45,990 of 720,000; ratios 0.896 / 1.121 / 1.254) and the census values (6/7 probes, 4 residues
per side; 6 of 15 selected degrader/glue pairs below 12), and expands the "WHAT IT MAY NOT CLAIM"
block to cover predicate non-identity, the hypothetical nature of the 6-7-against-12 comparison, the
missing joint covariance, the absence of an identified controlling variable, the zero-contact-band
selection that dropped 9MZA A/B and C/D, the single A2B2 assembly, the missing search closure, and
the absence of any selectivity estimate. It closes with a dated SUPERSEDED-RETAINED paragraph listing
the seven withdrawn claims, ahead of the pre-existing pre-2026-08-07 SUPERSEDED-RETAINED note, which
is preserved unchanged.

**Not patched, and why.**

* `systems/views/L3-publications.md`, `systems/views/paper-strength.md`,
  `systems/views/L2-rt-tcip.md`, `systems/views/L1-st-proximity.md` — all carry
  `kind: generated`, `generator: systems/systems_check.py`. They regenerate from the graph, so
  patching them by hand would be wrong. They are stale until the parent regenerates.
* `research/manuscripts/claim-coverage.json` — generated by
  `research/manuscripts/claim_coverage.py`; its row for the main manuscript records 123 sentences and
  62 with a number against the frozen text, which the revision changes. Regenerate rather than patch.
* `research/manuscripts/pinned-figures.json` — carries **no** TCIP entry (the manuscript's Appendix A
  item 1 says so explicitly). No entry was added: registering the ablation or size-axis figures is a
  pinning decision for the parent, not part of this narrowing, and adding one would create a new
  consistency obligation this lane cannot validate.
* `research/manuscripts/tcip/tcip-interface-floor-sizing.md` — outside the named scope and left
  unedited; see `../SIZING-MEMO-CARRYOVER.md` for the itemised list of withdrawn claims it still
  carries and three options for its owner.
* `research/modalities/nr4a3-tcip-reach.json` and its generated `.md` — the stale
  `verdict.★_the_named_effector` field is covered by a dated erratum in SI §S1. No patch: the
  contract forbids a producer or named-arm re-run, and rewriting a frozen artifact by hand would
  overwrite evidence.
