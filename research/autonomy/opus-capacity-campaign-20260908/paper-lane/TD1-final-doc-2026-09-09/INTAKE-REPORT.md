# TD1 residual 2: bounded mechanical intake

Frozen existing-return intake for `b14a84259363f8b18c98d7754a305892cd37f92b`, following `b42a5f23d44d33d84ad4be9ed9c73aaa22e13660`. Root independently adjudicates the scientific text against the four-item admission. This report verifies bindings, scoped data/code preservation and original-record limits; it is not another scientific review. No source lookup, classifier, scientific producer, checker, test, Git command or remote operation was run. Only generated intake artifacts under this work directory were written.

Four current shared files exactly match the author's intended after bytes. The fifth, publications.json, additionally preserves the exact earlier TCIP wording change already present at b42. The requested `depmap()` docstring correction is absent from both the delivered patch and current source. The Methods fixed identities are valid historical bindings, but the pending-integration caveat is stale and omits the changed producer. These are finite residuals for root's existing correction batch.

## Identity and patch evidence

The original `TD1-residual-2/HASHES.sha256` has 59 entries: **58 match; one self-entry mismatches**, with no missing file. Its self-entry claims `b8f6cb8d11f7f192f323704428d8ed3ba47e3ffbf03f728201287f21f032a228`; actual 6444 B file SHA is `3bfe4dc204012585cc9449babd8d6b3a4cc87cb5a02fc16a90e6cd2132388d2b`. This original mismatch is preserved; no list was altered or reconstructed.

All five author-before files match their edit-ledger bytes/hashes. Each supplied patch's before context matches those bytes; its intended after was materialized **only in memory** to compare with the author after manifest and actual current file. All five intended after hashes match the author ledger and `PATCHED-COPY-HASHES.sha256`. No patch, builder or producer was executed and no source file was written.

| Current file | Bytes | SHA256 | Binding |
|---|---:|---|---|
| TD1 main | 48268 | `9523555483f00cfee00b6cd4cf64f5615f57c011f1640297bf952399e742b3ba` | Exact declared author after |
| C: literature JSON | 39198 | `ae44e6e008275e1bb24426cd8b41810a59848b45839caac04eeb033e6e1658ae` | Exact author patch after |
| G: route JSON | 95906 | `cbb9c9ac67255ce86c57b91d2f448cc061f9801a2e856afada719d83f76f32cd` | Exact author patch after |
| GP: route producer | 56050 | `43447cd4a4756b0d0d45eef942a0a45fbe4bbee95537a592ef71663e1c0ad129` | Exact author patch after |
| MC: census main | 42114 | `d8fd8587bd3306e04ad41309c84efe5f32540f6242397b7c9f1d30a5f4039389` | Exact author patch after |
| Publications | 71006 | `fc55087c5de17c4fd5044dc7b87b9718e33d2b1c99d72c21c55d95dc0855d526` | Author TD1 change plus retained b42 TCIP change |

The main before is 41911 B / `12c082b3d4dc8dabedb4e7f7716435f175da88ea6e684c66f7d40de29942e8aa`, exactly the held focused 968 main. C/G/GP/MC before bytes also equal their held focused 968 copies. The author publication before is earlier than the held 968/b42 TCIP correction; this does not indicate current integration lost that correction. The README/map's five “UNAPPLIED” labels describe author handoff history; actual parent integration is demonstrated by the current comparisons.

Exact publication difference: author patch after **71000 B**, SHA `7f923cc0cbfaf0f101aaae6fb07f2f2e69eac8299092ddcbffb2a0ef6c515738`, versus current 71006 B changes only `PUB-TCIP.what_it_would_claim`. The changed phrase is:

> shared-stream covariance cannot be estimated from the held marginals and no design-aware interval for the ABLATION ratio exists

→

> empirical cross-arm covariance cannot be estimated from the retained marginal counts; no ratio interval is reported for that ABLATION

This exact before→after field difference also appears between the author's publication before and actual b42 publication bytes. Comparing **b42→b14** changes only `PUB-TXN-DEPENDENCY.outcome_potential_why`. No TCIP interpretation was re-adjudicated here.

## Scoped numerical, table and code preservation

- C: **230→230 leaves**, exactly **19 changed strings**, no added/removed or non-string changes. Query terms, all hit counts, identifiers, titles, assay/source quotations and all other leaves outside those named 19 string fields are unchanged. The changed fields are precisely recorded in the evidence JSON; historical reading/interpretation strings are not mislabeled as untouched source authority. The author reports 110 protected literals; this intake establishes full leaf-level preservation outside the actual 19 changes, without rerunning that checker.
- G: **1181→1181 leaves**, exactly **3 changed strings**, no added/removed or non-string changes: `RT-TXN-CDK.route_action`, `RT-CHAPERONE.observed`, `RT-CHAPERONE.verdict`.
- Publications: **416→416 leaves**, no non-string changes. Author TD1 patch changes one string; current-versus-author-before has that plus the separate inherited TCIP string.
- GP: AST structure is identical after masking string constants. Exactly **3 string constants** differ, and each complete new G string occurs exactly once among GP's constants. Arithmetic, control flow, numeric constants, memberships and other source structure are unchanged under this scoped comparison. Parsing was passive; GP was never imported or executed.
- Main: **66→78 table lines**. Twelve lines form the newly added identity table; only three pre-existing rows change, all in interpretation text: chaperone verdict, U4, U5. Every other old table row is identical, including the retained quantitative expression/dependency rows. Textual numeric-token multiplicities show no old token lost or reduced; this is not a statistical recomputation. The MC's **47 table lines are identical**.

## Finite residuals and map precision

1. **Requested R2 docstring remains unchanged.** Current `research/modalities/census_route_expression_grading.py` lines 45–51 still says the curated line “does not carry the fusion” and that near-universal sarcoma dependency is “evidence AGAINST a therapeutic window”. Exact before/current docstrings compare equal. The provided GP patch changes only the three later route strings. Root's admission explicitly named this docstring, so this is an actual omitted edit, not a new scientific question or a failure to rerun code.
2. **R4 integration caveat needs current status and GP coverage.** Main lines 475–478 still says two annotations are “prepared but not yet integrated”. Current C, G **and GP** already differ from the fixed historical table objects. The ten fixed identities remain valid historical bindings; the explanatory current-status caveat is incomplete/stale. R4 evidence below records the exact identities and replay boundaries.
3. **C1/C2 field-map locator typo.** The report table identifies Q1/Q2, but `patches/EDIT-LEDGER.json` and actual patch/current JSON identify Q2/Q6 for those first two edits. Q6 is actually corrected. Q1 is separately covered in the later bounded-outcome edits. This is a map correction, not a scientific implementation omission.
4. **Execution-scope wording requires qualification.** The report calls 07 exit 0 and says every check has measured sidecars; 07 has only valid-JSON stdout. Its numeric zero is narrative. Missing sidecars are not empty files. See the preserved original-run inventory below.

## R4 fixed identities and replay ceiling

All ten blob IDs printed in main lines 460–469 match actual retained focused-968 bytes and the reviewed-input manifest. They bind the explicitly named artifacts/producers as historical objects, not current mutable filenames. Current GP blob is `fe00fe381957bf248d774d0ed68cbf93c6674487`, versus fixed `18625608b378122adbe9dbcca16de370c357367b`; current G/C also differ. These changes are the already demonstrated annotation literals, not new numerical runs.

Passive source/input inspection finds **58 retained annotation records, 42+16**, with `gsm`, `class`, `annotation` fields. Stored class counts agree with their stored class labels; no classifier was replayed. All retained annotation strings are 64–125 characters, below the source serializer's 220-character cap. Source-visible extraction, import, classifier and call sites support a bounded possibility of replay against retained strings. That does not verify those strings against an absent original matrix or reconstruct all-probe intensities, mappings, reference distributions or per-line Chronos data. Exact source ranges, fixed hashes, averaging/rounding implementation and limits are retained in the independent `r4/` component.

The inspected code averages mapped nonmissing probes before standardization. Stored background mean/SD and gene averages are rounded to four decimals; percentiles use the unrounded gene average against the full probe distribution before their four-decimal display. `_zrow` consumes rounded stored inputs. Individual gene-read z values then round to four decimals, whereas list/signature scoring averages direct `_zrow` outputs and later displays group means at four decimals. These are distinct rounding boundaries, not a new computed result. The exact four named raw input filenames are absent from the retained-input manifest; this is a manifest-scoped absence, not a global filesystem claim.

## Original executions, failures and absent fields

Receipt component `receipts/ORIGINAL-RUN-RECEIPTS.md` and `receipts/receipt-map.json` preserves exact locators, all 38 stream/exit file identities, full small texts and scoped large-style output summaries. All 38 match applicable index bytes/SHA256. There are **14 named groups, 13 retained numeric exits: 9 zero and 4 nonzero**, plus 07 stdout with no exit sidecar.

| Original group | Actual preserved outcome |
|---|---|
| 01 apply main; 02 replay main | Each triplet reports exit 0. The two triplets are byte-identical. The separately claimed `cmp` has no independent captured record. |
| 03 build shared patches; 04 static integrity; 05 apply-check aggregate; 06 pycompile | Retained exit 0 in each group. 05's narrative describes five apply-check invocations; one aggregate group does not supply five separate exit sidecars. 06 has no stdout sidecar. |
| 07 JSON parse | Three-line valid-JSON stdout exists; no exit or stderr sidecar. Narrative exit 0 is not promoted to raw evidence. |
| 08 claims / changed-prose / readability | Retained exit 0 each. |
| 08 asymmetry / submission residue | Retained exit **2 / 2**, actual unrecognized-file-argument failures. |
| 08 current style / 09 before style | Retained exit **1 / 1**, reported output counts **121 / 99** errors. Before/after categories show bold-midsentence 53→63 and glyph 43→55; no cause or acceptability adjudication. 09 has no stderr sidecar. |

There are no full command sidecars; main command evidence is the original report's abbreviated commands/placeholders at lines 127–143. No precise invocation was reconstructed. Existing earlier source-binding, patch, CLI, builder/invariance, retrieval, reviewer decoding and source-object failures remain inherited history; none was reclassified or rerun by this intake. The empirical opposite-disagreement/EMC-selectivity claim remains parked under root's existing disposition. No gate or publication requirement is waived.

## Exact locators and read scope

CAP: `C:/Users/mcrae/.codex/review-workspaces/opus-capacity-sprint-20260907/capacity-campaign-20260908`. Current and author-before/patch files are under `CAP/observations/b14a8425/`; prior publications under `CAP/observations/b42a5f23/`; focused fixed files under `C:/Users/mcrae/Documents/Codex/2026-09-07/emc-research-orchestrator/work/td1-focused-verification-20260908/pinned/`. Full absolute paths, SHA256/Git blobs, original manifests, each actual leaf/string/row difference and patch-context results are in `bindings-and-deltas.json`, `additional-identities.json`, `receipts/` and `r4/`. Original sources remain immutable.

The named parent diff is 58375 B / `2a838d5df64b46e2fc4c72b4ff30ca5d0de8678d1094513f7c1a23cb47e287e0`; its six-file headers were inspected, while all five author patches were fully read and compared. The TD1 identity/check index is 84073 B / `4a06cb1534ec2f9a845e6f8351f48818812deb81ce3eb71f7bf24c5f5142983c`; the full B14 index is 844334 B / `07d7337c809c39f24cdca28eea98dc83fdb0a35294060c8b6d3437b9148e2ffc`, parsed only for relevant metadata. The 1087-file portfolio was not audited. An initial broad source-pattern search returned truncated output; no conclusions rely on omitted material. The R4 component records the narrow source sections actually inspected.
