# L1 — collection and adjudication against `CONTRACT-L1-deterministic-figure-jitter.md`

Collected 2026-09-08 10:34–10:40 UTC from retained originals. No re-run of L1's work, no restart.

## Child identity — from the transcript, full lifetime

| item | measured |
|---|---|
| child | `a050ed6a7259902e8` |
| original JSONL | `L1-executed-artifacts/ORIGINAL-CHILD-TRANSCRIPT-a050ed6a7259902e8.jsonl`, **162,406 B**, `cmp`-identical |
| model strings | **33 x `claude-opus-5`, 0 others** |
| tool pairs | **19** (self-reported 13 — a **seventh** consecutive undercount) |
| **full lifetime** | **10:30:27.375Z -> 10:33:48.138Z** (~3 m 21 s) |

## The change — one line

```
-        jitter = 0.12 * (hash(g) % 5 - 2)
+        jitter = 0.12 * (SHOW.index(g) % 5 - 2)
```

plus comments and a docstring paragraph. The offset now depends only on the gene's position in the
committed `SHOW` literal in the same file: no `hash()`, no unordered iteration, no clock, no RNG, no
environment. Value range is unchanged, {-0.24, -0.12, 0, +0.12, +0.24}.

## Verified by the parent, independently

- **Determinism, my own runs.** Three separate processes with `PYTHONHASHSEED` unset, `999` and `42`:
  all exit 0 and all produce sha256 **`130042b6afab8aea28874d37dd684cde96886ab25b488ab65b83dac391c439bd`**,
  128,250 B. L1's own four runs give the same single hash.
- **The defect was real, and I confirmed the control.** The **unmodified HEAD code**, run twice into
  the lane only, gives **`568e6970...`** and **`dd1ba2bd...`** — two different PNGs from identical
  inputs. This independently reproduces K1's measurement of the same defect.
- **No measured quantity moved.** Comparing HEAD's module against the edited one: `SHOW`, `LABEL`,
  `WINDOW_TIER` and `TIER_LABEL` all **identical**; the whole diff is **11 lines**, and the only
  removed line that is not a comment or docstring is the old `jitter` line itself. L1's probe also
  showed the evaluated-point list identical across 11 points and K1's NOT EVALUATED band
  (`[('SSTR2', 2), ('B4GALNT1', 3)]`) untouched.
- **Scope.** `git status` shows exactly the two authorised paths.

## Acceptance, adjudicated

1. **Deterministic offset from committed inputs — MET.**
2. **Determinism proven across processes and seeds — MET**, by L1's four runs and my three.
3. **No measured quantity changed — MET**, verified above rather than accepted.
4. **Legibility not regressed — MET.** L1 checked same-tier pairs: the closest pair by x
   (MCAM/EPHB4, dx 0.090) receives the **largest** vertical separation the scheme offers, dy 0.480;
   every other same-tier pair at dy 0.12 is >= 0.50 apart in x; the single same-offset pair
   (FGFR1/CD276, dy 0) is 1.85 apart on a 6.83-wide axis.
5. **Figure regenerated — MET.** `emc-surface-prioritization.png` `e3805cf1...` / 126,861 B ->
   `130042b6...` / 128,250 B.
6. **No manuscript edit — MET**, and none was needed; the caption stays factually correct.
7. **Gates — MET, and NOT all green.** My own run: six gates exit 0; **`lint_citations` exits 1**,
   pre-existing and repo-wide, naming L1's two edited files **0 times**. Its failures are article-type
   claims lacking cached PubMed metadata in unrelated campaign reports, which would need a networked
   fetch. No gate weakened, relaxed, reordered or edited.
8. No stop condition fired.

## A correction L1 made to my own record

My K1 adjudication quoted the pre-K1 PNG (`30b1c25b...` / 91,013 B) as the "before" hash. For L1 the
correct baseline is the **post-K1 committed** file, `e3805cf1...` / 126,861 B. L1 flagged this rather
than inheriting my number. Both hashes are right for their own revision; the K1 record is not wrong,
but the figure has now had two successive baselines and this note keeps them distinct.

## What this closes, and what stays open

**Closes:** peer review **Minor 16/17**. The figure now reproduces byte-identically from its own
committed code, so a future figure diff is readable and a published figure can be regenerated by a
reader.

**Still open on this paper:** `lint_citations` is red (pre-existing); the abstract's n = 4 / n = 6
residue; **item 44**, blocked and recorded with its reopening condition in
`BLOCKER-surface-targets-item44-surfaceome-overlap.md`; and the DFSP-only sensitivity analysis, which
remains undone and out of scope. **This is not publication acceptance.**

## Retention

`/tmp/claude-0/l1-lane/` **intact, nothing deleted**, pending an exact-directory receipt. In-repo copy
`L1-executed-artifacts/` with a self-exclusive manifest.
