# T1 — served-model receipt

Parent verification command run at **Tue Sep  8 11:35:31 UTC 2026** (from `date -u` in the same command, **not** a composition
estimate), after the T1 contract commit `b6586bc9`. Source:
`…/subagents/agent-a16aa872bfa62b3bb.jsonl`.

| item | observed |
|---|---|
| child | `a16aa872bfa62b3bb` |
| requested | `claude-opus-5`, **medium** |
| route | existing first-party saved subscription — no paid fallback, overage, credits or GPU |
| writes to repo | **none** — lane only |

Parsed from transcript model fields, **not inferred from contract text**. Full lifetime span and final
tool-call count come from the original JSONL at collection.

## Why this lane exists — a correction to my own reasoning

In `R1-R2-MODEL-START-RECEIPTS.md` I listed care-delivery's field-coverage question as bounded because
**"no such route is admitted"**. **That was an assumption, not a measurement.** The absence of a prior
tiny contract is not an unavailable capability, and ordinary public-source method questions fall under
the standing workstream authority. **The claim is withdrawn**, and the two readings I then took are
recorded in the T1 contract:

- `clinicaltrials.gov` -> **`EGRESS_BLOCKED`**, measured, not to be retried.
- `WebSearch` -> secondary sources stating `query.term` covers all study fields **including eligibility
  criteria**.

⭐ So one of my four "measured grounds" for a low worker count was **not** measured. The other three
stand as stated. This is the falsifiability I claimed for them being exercised.

**Live counts:** research children **1** (T1); helpers **1** (the legacy Bash waiter — computes
nothing, **not** research). R1 and R2 are terminal and collected; their proposals are reviewable
candidates awaiting the scientific coordinator's integration decision, and **the shared manuscript,
tier, registry and references remain unchanged** until that decision.
