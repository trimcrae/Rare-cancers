# Y1 - served-model receipt

Parent verification command run at **Tue Sep  8 12:22:55 UTC 2026** - from `date -u` in the same command, not composed. After
the Y1 contract commit `49d7748f`.

| item | observed |
|---|---|
| child | `a47ee9339d1263157` |
| requested | `claude-opus-5`, **medium** |
| route | existing first-party saved subscription - no paid fallback, overage, credits or GPU |
| writes shared? | **none** - lane only, read-only git |

Parsed from transcript model fields, **not inferred from contract text**. Actual execution is confirmed
by the transcript's own tool activity, not by the existence of a contract.

**Live counts:** research children **1** (Y1); helpers **1** (the legacy Bash waiter - computes
nothing, **not** research). X1 is terminal and collected; W1, W2, W3, U1, U2, R1, R2, V1 are terminal.

## Why this lane, and why now

It is the **only remaining item in the campaign that is both unblocked and substantive**: an
**unsourced load-bearing claim in a live manuscript**, surfaced by X1's own honesty in flagging what it
could not source rather than inventing a citation.

The rest of the rank is genuinely accounted for, each on measured ground:

- **repurposing** - integrated; the version blocker is **closed**; what remains is registry
  conformance (clinical governance), the repo-wide citation gate, and the 25-vs-22 reference-count
  divergence.
- **ATR** - the X1 candidate awaits the **coordinator's** concrete decision; its remaining unknowns are
  either that decision's to take or genuinely unresolvable from committed inputs.
- **surface-targets** - set down: item 44 blocked with a **tested** route, the abstract residue does
  not fit at 200/200, DFSP recomputation out of scope.
- **mtap-prmt5** - the register attribution is fixed; publisher confirmation and cross-version identity
  remain **measured** blocks.
- **care-delivery** - the instrument is now named in the paper; coverage is unresolved and its
  documentation host is **measured** as blocked.
- **neoantigen** - the alleged defect was **already corrected**; the stale flag was closed.
- **tcip** - the naming hazard is **already disclosed in its own manuscript**.
- **fusion-output / W25, NR4A Perspective, P6, ASO / RNA frozen assets** - held.

⛔ **No census, duplicate review, blocked-route replay or filler was created to claim occupancy.**
