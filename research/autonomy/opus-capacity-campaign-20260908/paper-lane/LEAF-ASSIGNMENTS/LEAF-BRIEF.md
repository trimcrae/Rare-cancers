# LEAF WORKER BRIEF — endpoint source-validation leaves, wave of 2026-09-08 ~18:55Z

You are ONE leaf worker. You own EXACTLY ONE question and ONE named NCT group. Nothing else.

## Immutable shared inputs — read only, never copy, never re-fetch
- Cache: `/tmp/claude-0/-home-user-Rare-cancers/8ecd0f49-96ba-5dcf-b11a-af5e48bdec71/scratchpad/ctg-cache-216bd1b5/`
  12 payloads + `_manifest.json` + `SHA256-MANIFEST.txt`, from branch `literature-cache` @ `216bd1b5fb25a56b90ef3cc2373e1fe68322708f`, 157,268,733 payload bytes.
- ⛔ **NO network request of any kind.** No ClinicalTrials.gov call, no re-download, no new cache copy.
- ⛔ Do NOT run any producer, any manuscript build, any test suite, any gate, any preflight.
- ⛔ Do NOT edit any manuscript, artifact, producer, graph file or frozen packet.
- ⛔ Do NOT commit, do NOT push, do NOT create a branch or worktree that writes tracked files.
- Verify the cache once with `sha256sum -c SHA256-MANIFEST.txt` before reading; report the exit code.

## Owner fences
- Jobs 1–3 (measurement contract / global identity / arm-level disease+phase) retain their
  extraction-contract, global-identity and mapping-method tasks. You do NOT redo them and you do NOT
  restate their global counts as your own. Their outputs are context, not your findings.
- Your rows are yours alone. No other leaf holds your NCTs. Do not read or reason about NCTs outside
  your assigned list, except to say "outside my group".
- Cross-shard duplicate reconciliation is the parent's, AFTER keys come back. Do not attempt it.

## Output
One markdown file plus at most one TSV, under
`research/autonomy/opus-capacity-campaign-20260908/paper-lane/LEAF-OUT/` with the exact filename your
task names. Untracked files only. Every load-bearing number must carry an exact source pointer
(payload file, NCT, outcome index, group id, class index) so the parent can re-derive it.

## Honesty rules that override any instinct to produce a tidy result
- A row your assigned evidence cannot settle is `UNRESOLVED`, with the reason. That is a result.
- Never invent a denominator, a category, a patient count or a trial fact.
- Report what you actually checked and what you did not reach. Partial coverage is reportable;
  fabricated completeness is not.
- Assert nothing about efficacy, safety, selectivity, therapeutic window or clinical readiness.
- The endpoint and mortality manuscripts are PARKED. You are not correcting a paper. You are
  establishing what the source records do and do not support.
