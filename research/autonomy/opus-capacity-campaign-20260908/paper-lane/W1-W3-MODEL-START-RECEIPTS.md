# W1, W2, W3 — served-model receipts

Parent verification command run at **Tue Sep  8 11:56:34 UTC 2026** — from `date -u` in the same command, not composed. After
the contract commit `f5a16b5b`.

| lane | child | requested | writes shared? | scope |
|---|---|---|---|---|
| **W1** | `afb6b8e0c83a21fac` | `claude-opus-5` medium | **none** | assemble the proposed Supplementary Table S3 from the digest-pinned committed JSON |
| **W2** | `a89e97a25d998f23d` | `claude-opus-5` medium | **none** | what Panel A's dashed boxes and ticks encode, from generator and data |
| **W3** | `a1c059e448e91c63b` | `claude-opus-5` medium | **none** | the missing bibliographic fields for PMID 11679947 only |

Parsed from transcript model fields, **not inferred from contract text**. Route: existing first-party
saved subscription — no paid fallback, overage, credits or GPU.

**Research children executing: 3.** Helper: **1** (the legacy Bash waiter — computes nothing, **not**
research). Each writes only to its own lane; **parent integration alone owns shared files**, and
**one owner per writing scope** holds trivially since none of the three writes anything shared.

## Each is a dependency the review actually identified

- **W1** ← U1 found R1's unknown **U3 partly resolvable**: Table S3 is buildable from committed inputs.
- **W2** ← U1 found Panel A has **no key at all** and flagged its own reading of the dashed boxes and
  ticks as **inference, not pixels**. W2 must source that from the generator — and **may not claim code
  proves rendered legibility**. Where the code is silent, the answer is **unresolved**.
- **W3** ← U1 resolved half of R1's **U6** (Sjögren 2003, already committed) and found Okamoto 2001
  genuinely missing. W3 must **check the repository first** — `11679947` already appears in a committed
  report — and **reuse rather than retrieve** if the fields are there.

## Two corrections carried in

⭐ **U1's actual tool-call count is 24, not the 21 it self-reported** — 24 of its 35-call budget. That
is the **eighth** self-report undercount in this campaign; the transcript remains authoritative.

⭐ **U1's possible Figure 1 cross-reference issue is hypothetical and absent from both drafts**, as my
own check found (`"Figure 1"` occurs **0** times in the committed manuscript and **0** in R1's
proposal). **It is not being turned into a repair task**, and no lane is pointed at it.

Also recorded: the ATR PNG's retained-RG-fraction axis and 177-nt seam labels are corroborated, while
**its symmetric-sweep quantities are not rendered** — consistent with U1's finding that citing Figure 1
for the sweep would be false.
