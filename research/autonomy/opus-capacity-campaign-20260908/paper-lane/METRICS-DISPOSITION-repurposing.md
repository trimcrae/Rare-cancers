# submission-metrics.json - disposition of the repurposing main_words delta

Asked: the committed metrics at `d85d0e40` show repurposing `main_words` **5619**, while the retained
once-only integration check stdout reports **6037** and says it wrote the JSON. Reconciled here **by
inspection only - no check was rerun, no shared file restored, nothing unrelated staged.**

## What actually happened

| pin | `rows[repurposing].measured.main_words` |
|---|---|
| `6eb48cce` (before integration) | **5619** |
| **`d85d0e40`** (the integration commit) | **5619** |
| **`30689429`** (the next commit) | **6037** |
| `HEAD` | **6037** |

`submission_metrics.py` rewrites that JSON as a **side effect** of running. At `d85d0e40` I staged
**only** the manuscript and the paper-lane records, so the regenerated JSON stayed **unstaged** - it was
not lost, not failed, and not silently dropped. It was then staged **one commit later**, at `30689429`,
whose message records it as *"regenerated by the single authorised check run on the integrated
repurposing manuscript"*.

**So the derived update matches the already-executed check exactly**: 6037 is the value that check
printed, and it is what HEAD carries. The working tree is **clean**.

⛔ **Nothing here indicates a scientific failure or loss** - a generated file being briefly uncommitted
is a staging-order artifact, not evidence about the manuscript. The one-run-only rule was honoured:
**the check was not rerun to produce this reconciliation.**
