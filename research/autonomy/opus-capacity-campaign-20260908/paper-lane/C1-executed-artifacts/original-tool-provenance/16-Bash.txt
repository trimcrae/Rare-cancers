287:{"cycle_id": "CYC-0105-e41d184e", "utc": "2026-09-04T17:40:00Z", "path": "research/manuscripts/tests/test_emc_host_factor_model.py", "what_changed": "Three test functions ADDED (paper-guards tier 963 -> 966 of 1000): (1) an evidence row with status association_only yields compartment status ASSOCIATION_ONLY, carries the hazard-ratio range, and pins every reduction and averted-share range to [0, 0]; (2) an evidence row whose compartment is neither A nor B (a corroborating citation) is never modelled but its PMID is still anchor-checked by check_anchors; (3) a row-level endpoint_caveat is carried onto the output. No existing test was edited, weakened, removed or loosened; the 15 prior functions pass unchanged (18/18).", "why": "AUT-220 (RT-HOST-FACTOR): emc_host_factor_model.py gained an association_only evidence status so that a sarcoma-specific hazard ratio for HAVING a factor (sarcopenia OS HR 1.91; statin PFS aHR 0.64) can be recorded in the inputs WITHOUT being modelled as an intervention effect. Governed because **/tests/** is governed.", "self_serving_check": "ANSWERED. This cycle was not blocked by any bar, gate or test: it took a research item (read a retrieval, enter effect sizes, run a model) and no clause of publish_bar, no preflight gate and no existing test refused anything it did. DIRECTION: the model change makes the model claim LESS, not more -- an evidence row that previously would have needed relative_risk_reduction values (and so would have had to be either omitted or given a number) can now be recorded at a forced ZERO with its hazard ratio visible. The new tests assert that zero; they cannot make any host-factor number larger, cannot make a paper read as more ready, and cannot make this or any cycle report success more easily. The tier budget was not raised; the tier has 34 functions of headroom after this change."}
=== ledger ===
2197:cts (terminal events raw and classified, the decomposition and its inputs, relative survival), 5 producing scripts (`emc_terminal_events`, `emc_mortality_decomposition`, `emc_relative_survival`, `emc_host_factor_model`, `emc_supportive_effect_transfer`), 4 test modules and the five `scripts/lit_*_probe.py` with their common module.\n\n⛔⛔ AND THE METHOD IS THE FINDING, BECAUSE THREE READINGS WERE WRONG BEFORE ONE WAS RIGHT — ALL THREE FOR THE SAME REASON, AND THIS ROW ALREADY RECORDED IT ONCE AS 'A DI