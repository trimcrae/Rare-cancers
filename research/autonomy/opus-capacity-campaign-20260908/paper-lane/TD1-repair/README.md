# TD1 author repair — one frozen candidate for the parent to integrate

**2026-09-08.** Sole TD1 author-repair owner, disjoint from the live P-ST owner. ⛔ **No commit, no
push** — the parent integrates. Nothing was spawned.

## Disposition delivered

Root accepted all 11 findings of the single independent final review, held the candidate frozen at
`9f571e81`, and **parked** its central empirical "opposite disagreement" claim: these unpaired
expression and dependency data do not measure such a relationship. This lane delivers the narrower
descriptive synthesis of two distinct evidence streams, applying all eleven corrections in **one batch**.

⭐ **The narrower revision stands free of every unmet stronger claim.** No section of it depends on an
observed expression–dependency relation, on GPL3290 as corroboration, on demonstrated paralogue
compensation, on chaperone clientship, or on demonstrated absence of selectivity. **There is no residual
blocker requiring this version to be parked.** The three residual items — the GPL3290 interpretation
hold, an unknown retraction-sweep status for the new reference list, and the repository's house-style
glyph convention — are stated limits and process items, not scientific blockers, and are listed in
`FINDING-MAP.md` §Residual.

## Contents

| file | what it is |
|---|---|
| `FINDING-MAP.md` | F1–F11 → the exact change made for each, with the object identities and the residual items |
| `CHANGED-FIELD-CODE-MAP.md` | the dated exact changed-field / changed-code map for the six shared files, the method that makes it provable, and what was deliberately not changed |
| `patches/*.patch` | six unified diffs for **parent-owned** shared files — **not applied**. `git apply` from the repository root |
| `patches/EDIT-LEDGER.json` | per-file original/patched bytes and SHA256, and per-field old/new lengths and hashes |
| `INVARIANCE-EVIDENCE.json` | numeric/membership invariance evidence: 441,602 checks, 0 failures |
| `BEFORE-…dependency.md` | the frozen input, `fe316b44…`, 20,924 B |
| `frozen/…dependency.md` + `frozen/HASHES.sha256` | the frozen repaired candidate, `12c082b3…`, 41,911 B |
| `checks/` | every executed check, every attempt, with real streams and real exits; `checks/README.md` is the attempt log |

## The one file edited in place

`research/manuscripts/dependency/emc-transcriptional-proteostatic-dependency.md` — owner-owned.

- **in:** 20,924 B · `fe316b44d44b7f16cbb62ea7451dd4c3acff1a8cc2edc0dd73932e98649c9398`
- **out:** 41,911 B · `12c082b3d4dc8dabedb4e7f7716435f175da88ea6e684c66f7d40de29942e8aa` · Git blob
  `60fa27c312faefb106f627aaa085c86b8d37b645`

## Verification actually performed

- **230 source/text bindings, exit 0.** Every number, membership, coverage figure, comparator count,
  exclusion, threshold, cohort rule and literature bound in the manuscript is checked against the live
  artifacts. The three expression/dependency artifacts are byte-identical to the hashes the reviewer
  recorded as its own inputs (`123bd05a…`, `d88bed62…`, `5bc3c80c…`).
- **The fourteen conditional-arithmetic readings were independently re-derived** here and agree with the
  retained reviewer attempt-2 output to < 1e-9 on every *p* and < 1e-6 on every interval bound, from a
  routine calibrated on the Cauchy identity. The three Bonferroni-14 passes and the fact that **none is
  on both platforms** were re-derived, not copied.
- **441,602 invariance checks, exit 0** over the six shared-file patches, including 393,752 non-string
  leaves proven unchanged and exactly 22 string changes, all declared. Each patch was applied to a
  pristine copy outside the repository to prove it applies.
- **Manuscript-scoped lints:** `lint_claims` 0, `lint_changed_prose` 0, `lint_readability` 0,
  `lint_asymmetry` 0, `lint_submission_residue` 0; `lint_style` 1 on both the input and the candidate
  (house-style glyphs). `lint_citations`/`lint_citation_types`/`lint_consistency` exit 1 repo-wide with
  **no hard error naming this manuscript**.

⛔ No producer, raw reanalysis, simulation, experiment, source acquisition, new baseline review, corpus
preflight or publication was run. None of the above is a commit gate or a scientific clearance.

## For the parent

1. Integrate the six patches in `patches/`, or reject any of them; they are independent of each other.
2. `systems/graph/publications.json` is included beyond the literal F10 list because it carried the
   parked claim verbatim. `systems/views/L3-publications.md` and `L2-rt-chaperone.md` are generated from
   it and will need the parent's normal regeneration afterwards; this lane did not touch generated views.
3. `research/manuscripts/modality-census/cancer-modality-census.md:186` still carries the withdrawn
   "most concordant elevation the whole census found" superlative. It is another paper's file and was
   left untouched to avoid overlap; it needs an owner.
4. The fifteen reference identifiers are **NOT SWEPT** against the 2026-09-01 retraction sweep. That is
   UNKNOWN, not clean, and needs a networked sweep this lane is fenced from.
