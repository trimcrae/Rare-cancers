Empty by design. No change to research/modalities/emc_condensate_calvados.py was required.
Its --selftest passes 78/78 in this container (checks/10, exit 0) and score() reproduces the
committed panel exactly from the retained per-run nu (checks/11, exit 0), so the frozen module
needs no patch to support the analysis that was performed.
