# C1 — finite contract receipt, child ID, model and start (verified from the child transcript)

| field | value |
|---|---|
| child id | `a07b78237ee438258` |
| contract | `paper-lane/CONTRACT-C1-competing-share-consequence.md`, recorded **before** dispatch, with the dated scope append that withdraws two of its own claims |
| dispatched | 2026-09-08 ~07:29 UTC; verification read 07:30 UTC (`date -u`) |
| **observed model set in transcript** | **`['claude-opus-5']`** — single value, by regex over `"model":"…"` across the raw child transcript, not inferred from the dispatch parameter |
| effort / route | medium, existing first-party subscription — no paid fallback, no overage |
| session | `session_01Eui7FVgatEXAwt2N35yHH6` (unchanged) · deadline `2026-09-09T02:37:19Z` (unchanged) |

**Finite contract, as dispatched:** step 1 verifies the source estimator's provenance from `direct_cause_split`'s
**stored value or exact numerator/denominator** — never from the rounded "21.7 per cent" display — and **stops
with an admissible result** if the committed record shows the within-series estimator was deliberate (no second
variant, no sensitivity sweep). Step 2, only if step 1 does not stop it, changes **only** the competing-share
input with every other assumption held fixed, and reports each output beside its current value with the ratio
plus a per-conclusion survives/does-not statement. Scratch-only execution; durable artifacts to
`/tmp/claude-0/c1-retained/`, hash-verified **before** any cleanup and not deleted; parent alone collects.
Excluded inputs named, not fetched (`factors_not_entered`: diabetes/metformin, hypertension — network).
**Interpretation limits binding on its report:** the quantity is a cohort death fraction — **not population
mortality risk, not a causal treatment effect, not a bound on any antitumour argument** — and the run
establishes **no clinical benefit**. No manuscript, graph or repository write; every correction remains an owner
act.

**Counts at this reading, helpers separate:** active research children **1** (C1). Helpers **1** — the legacy
Bash waiter "Block until pass 2 finishes", which computes nothing and is not research. A1/A2/A3 completed and
collected; B1 killed with its original transcript and all 17 tool responses retained.
