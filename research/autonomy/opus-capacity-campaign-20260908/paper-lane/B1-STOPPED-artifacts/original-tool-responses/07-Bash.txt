13:  `CLOSED-WORK.md` are unreadable there. Read the working tree. Treat the whole tree as
132:  `scripts/tests/test_the_dep_probe_asks_the_interpreter_that_runs_the_tests.py` records incident
153:(34 requirement rows) and mutation-probed each weak row. **Every WEAKER and NOT-IMPLEMENTED row is
177:"5–8 view pages". Measured range on W26c's four fields is **3 to 12** (`ST-REPURPOSING.limitations[1]`
215:### ⛔ Lane 2 is CLOSED, and its last surviving claim is WITHDRAWN — measured 2026-09-08T04:38Z
236:Lane 2 is finished on this substrate: two withdrawn findings (endothelial W02i, fibro/ECM here), one
239:dispatch further lane-2 statistics on `emc-expression-panels.json`.** No new lane-2 worker, and no
333:  (`lint_readability` splits cleanly: its baseline-anchored errors dissolve, its 60-word-sentence
342:  the `dataStatusBanner` ERROR fires only when `dataStatus === "SAMPLE_SYNTHETIC"`, so deleting the
343:  banner outright on the live `partial-curated` registry is a **silent pass**.
444:  ("three cohorts on three platforms") than its own manuscript ("the two readable array platforms").
549:  `[P1]`, the residue/readability baselines) are growth-guarded by construction. ⚠ `lint_style.TARGETS`
550:  covers **8 of the 26** `publications.json` `document.file` entries, and `lint_readability.py:297` and
759:  That is the mirror of `lint_readability`: **an in-source constant blocks the escape when the check
769:  universal over an **empty set** (0 artifacts carry its `BANNER_KEY`); `continuity` has **no write
985:⛔ **REFUTED (lead 2, ALK/ROS1 screen hit).** `publications.json:288` says *"its named kinases have no probe on
987:that owns the read — says **ROS1 is readable on BOTH** platforms, ALK on GPL6244, and the brigatinib target
992:five graph files. ⭐ The correction does **not** turn a null into a positive: where readable, the kinase sits