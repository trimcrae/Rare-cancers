python3 build.py   # 7 exact-match anchor assertions -> AFTER.md  -> PROPOSED-UNAPPLIED-five-unread-references.diff
python3 build2.py  # 4 exact-match anchor assertions -> AFTER2.md -> PROPOSED-UNAPPLIED-zaltoprofen-parent-histology-ADDS-REFERENCE.diff
Each script exits 3 if any anchor matches other than exactly once, so no edit can land on a near-miss.
Both build from the committed manuscript, unmodified; both diffs are UNAPPLIED.
