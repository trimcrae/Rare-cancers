git apply --check --verbose <lane>/scope-abstract-recompute.patch
(patch hand-written with an incorrect @@ hunk-count header)
⛔ RECONSTRUCTED FROM THE SESSION TRANSCRIPT. The original stdout/stderr/exit_code bytes of this
attempt were overwritten by the later successful attempt in checks/07-git-apply-check, because that
attempt reused the same directory. The overwrite is recorded here rather than concealed; the values
below are the exact text and exit status the transcript shows for this attempt.
