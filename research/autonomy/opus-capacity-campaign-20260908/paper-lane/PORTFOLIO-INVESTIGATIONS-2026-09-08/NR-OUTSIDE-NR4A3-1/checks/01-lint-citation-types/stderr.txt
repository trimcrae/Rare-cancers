::error::research/autonomy/opus-capacity-campaign-20260908/paper-lane/P6-PUB-NR-OUTSIDE-NR4A3-paper-step.md:172 TYPE CLAIM WITH NO CACHED METADATA — prose calls PMCID PMC9489176 "a review" ("review (PMC9489176") and there is no row for PMCID PMC9489176 in research/manuscripts/citation-article-types.json. ⛔ THIS IS NOT A PASS — the guard cannot check a claim it has no metadata for, so it fails. Fetch it in an interactive session (the connector is the fetcher, this gate is offline): PubMed MCP `convert_article_ids` (id_type='pmcid') if you have only a PMCID, then `get_article_metadata` with the PMID, and copy `identifiers`, `journal.iso_abbreviation`, `publication_date.year`, `title` and `article_types` into a record verbatim. The file's `_how_to_refresh` carries the same steps.
::error::research/autonomy/opus-capacity-campaign-20260908/reports/W04b-diagnostic-delay-interval.md:168 TYPE CLAIM WITH NO CACHED METADATA — prose calls PMID 29886600 "a case report" ("case reports, one (Yue 2018, PMID 29886600") and there is no row for PMID 29886600 in research/manuscripts/citation-article-types.json. ⛔ THIS IS NOT A PASS — the guard cannot check a claim it has no metadata for, so it fails. Fetch it in an interactive session (the connector is the fetcher, this gate is offline): PubMed MCP `convert_article_ids` (id_type='pmcid') if you have only a PMCID, then `get_article_metadata` with the PMID, and copy `identifiers`, `journal.iso_abbreviation`, `publication_date.year`, `title` and `article_types` into a record verbatim. The file's `_how_to_refresh` carries the same steps.
::error::research/autonomy/opus-capacity-campaign-20260908/reports/W04b-diagnostic-delay-interval.md:478 TYPE CLAIM WITH NO CACHED METADATA — prose calls PMCID PMC12659415 "a case report" ("case reports listed in `SILENT_CASES` (most have PMC IDs and are open access — PMC12659415") and there is no row for PMCID PMC12659415 in research/manuscripts/citation-article-types.json. ⛔ THIS IS NOT A PASS — the guard cannot check a claim it has no metadata for, so it fails. Fetch it in an interactive session (the connector is the fetcher, this gate is offline): PubMed MCP `convert_article_ids` (id_type='pmcid') if you have only a PMCID, then `get_article_metadata` with the PMID, and copy `identifiers`, `journal.iso_abbreviation`, `publication_date.year`, `title` and `article_types` into a record verbatim. The file's `_how_to_refresh` carries the same steps.
::error::research/autonomy/opus-capacity-campaign-20260908/reports/W04b-diagnostic-delay-interval.md:478 TYPE CLAIM WITH NO CACHED METADATA — prose calls PMCID PMC10908772 "a case report" ("case reports listed in `SILENT_CASES` (most have PMC IDs and are open access — PMC12659415 …, PMC10908772") and there is no row for PMCID PMC10908772 in research/manuscripts/citation-article-types.json. ⛔ THIS IS NOT A PASS — the guard cannot check a claim it has no metadata for, so it fails. Fetch it in an interactive session (the connector is the fetcher, this gate is offline): PubMed MCP `convert_article_ids` (id_type='pmcid') if you have only a PMCID, then `get_article_metadata` with the PMID, and copy `identifiers`, `journal.iso_abbreviation`, `publication_date.year`, `title` and `article_types` into a record verbatim. The file's `_how_to_refresh` carries the same steps.
::error::research/autonomy/opus-capacity-campaign-20260908/reports/W04b-diagnostic-delay-interval.md:478 TYPE CLAIM WITH NO CACHED METADATA — prose calls PMCID PMC10519304 "a case report" ("case reports listed in `SILENT_CASES` (most have PMC IDs and are open access — PMC12659415 …, PMC10519304") and there is no row for PMCID PMC10519304 in research/manuscripts/citation-article-types.json. ⛔ THIS IS NOT A PASS — the guard cannot check a claim it has no metadata for, so it fails. Fetch it in an interactive session (the connector is the fetcher, this gate is offline): PubMed MCP `convert_article_ids` (id_type='pmcid') if you have only a PMCID, then `get_article_metadata` with the PMID, and copy `identifiers`, `journal.iso_abbreviation`, `publication_date.year`, `title` and `article_types` into a record verbatim. The file's `_how_to_refresh` carries the same steps.
::error::research/autonomy/opus-capacity-campaign-20260908/reports/W04b-diagnostic-delay-interval.md:478 TYPE CLAIM WITH NO CACHED METADATA — prose calls PMCID PMC9527174 "a case report" ("case reports listed in `SILENT_CASES` (most have PMC IDs and are open access — PMC12659415 …, PMC9527174") and there is no row for PMCID PMC9527174 in research/manuscripts/citation-article-types.json. ⛔ THIS IS NOT A PASS — the guard cannot check a claim it has no metadata for, so it fails. Fetch it in an interactive session (the connector is the fetcher, this gate is offline): PubMed MCP `convert_article_ids` (id_type='pmcid') if you have only a PMCID, then `get_article_metadata` with the PMID, and copy `identifiers`, `journal.iso_abbreviation`, `publication_date.year`, `title` and `article_types` into a record verbatim. The file's `_how_to_refresh` carries the same steps.
::error::research/autonomy/opus-capacity-campaign-20260908/reports/W04b-diagnostic-delay-interval.md:478 TYPE CLAIM WITH NO CACHED METADATA — prose calls PMCID PMC9044308 "a case report" ("case reports listed in `SILENT_CASES` (most have PMC IDs and are open access — PMC12659415 …, PMC9044308") and there is no row for PMCID PMC9044308 in research/manuscripts/citation-article-types.json. ⛔ THIS IS NOT A PASS — the guard cannot check a claim it has no metadata for, so it fails. Fetch it in an interactive session (the connector is the fetcher, this gate is offline): PubMed MCP `convert_article_ids` (id_type='pmcid') if you have only a PMCID, then `get_article_metadata` with the PMID, and copy `identifiers`, `journal.iso_abbreviation`, `publication_date.year`, `title` and `article_types` into a record verbatim. The file's `_how_to_refresh` carries the same steps.
::error::research/autonomy/opus-capacity-campaign-20260908/reports/W04b-diagnostic-delay-interval.md:478 TYPE CLAIM WITH NO CACHED METADATA — prose calls PMCID PMC6449903 "a case report" ("case reports listed in `SILENT_CASES` (most have PMC IDs and are open access — PMC12659415 …, PMC6449903") and there is no row for PMCID PMC6449903 in research/manuscripts/citation-article-types.json. ⛔ THIS IS NOT A PASS — the guard cannot check a claim it has no metadata for, so it fails. Fetch it in an interactive session (the connector is the fetcher, this gate is offline): PubMed MCP `convert_article_ids` (id_type='pmcid') if you have only a PMCID, then `get_article_metadata` with the PMID, and copy `identifiers`, `journal.iso_abbreviation`, `publication_date.year`, `title` and `article_types` into a record verbatim. The file's `_how_to_refresh` carries the same steps.
::error::research/autonomy/opus-capacity-campaign-20260908/reports/W04b-diagnostic-delay-interval.md:478 TYPE CLAIM WITH NO CACHED METADATA — prose calls PMCID PMC5351361 "a case report" ("case reports listed in `SILENT_CASES` (most have PMC IDs and are open access — PMC12659415 …, PMC5351361") and there is no row for PMCID PMC5351361 in research/manuscripts/citation-article-types.json. ⛔ THIS IS NOT A PASS — the guard cannot check a claim it has no metadata for, so it fails. Fetch it in an interactive session (the connector is the fetcher, this gate is offline): PubMed MCP `convert_article_ids` (id_type='pmcid') if you have only a PMCID, then `get_article_metadata` with the PMID, and copy `identifiers`, `journal.iso_abbreviation`, `publication_date.year`, `title` and `article_types` into a record verbatim. The file's `_how_to_refresh` carries the same steps.
::error::research/autonomy/opus-capacity-campaign-20260908/reports/W04b-diagnostic-delay-interval.md:478 TYPE CLAIM WITH NO CACHED METADATA — prose calls PMCID PMC5011171 "a case report" ("case reports listed in `SILENT_CASES` (most have PMC IDs and are open access — PMC12659415 …, PMC5011171") and there is no row for PMCID PMC5011171 in research/manuscripts/citation-article-types.json. ⛔ THIS IS NOT A PASS — the guard cannot check a claim it has no metadata for, so it fails. Fetch it in an interactive session (the connector is the fetcher, this gate is offline): PubMed MCP `convert_article_ids` (id_type='pmcid') if you have only a PMCID, then `get_article_metadata` with the PMID, and copy `identifiers`, `journal.iso_abbreviation`, `publication_date.year`, `title` and `article_types` into a record verbatim. The file's `_how_to_refresh` carries the same steps.
::error::research/autonomy/opus-capacity-campaign-20260908/reports/W06-diagnostic-delay-molecular-confirmation.md:219 TYPE CLAIM WITH NO CACHED METADATA — prose calls PMID 27819877 "a case report" ("case reports and to the SEER-based series (Kemmerer 2018, PMID 27819877") and there is no row for PMID 27819877 in research/manuscripts/citation-article-types.json. ⛔ THIS IS NOT A PASS — the guard cannot check a claim it has no metadata for, so it fails. Fetch it in an interactive session (the connector is the fetcher, this gate is offline): PubMed MCP `convert_article_ids` (id_type='pmcid') if you have only a PMCID, then `get_article_metadata` with the PMID, and copy `identifiers`, `journal.iso_abbreviation`, `publication_date.year`, `title` and `article_types` into a record verbatim. The file's `_how_to_refresh` carries the same steps.
::error::research/autonomy/opus-capacity-campaign-20260908/reports/W09-care-access-referral.md:160 TYPE CLAIM WITH NO CACHED METADATA — prose calls PMID 41689087 "a case report" ("case report (PMID 41689087") and there is no row for PMID 41689087 in research/manuscripts/citation-article-types.json. ⛔ THIS IS NOT A PASS — the guard cannot check a claim it has no metadata for, so it fails. Fetch it in an interactive session (the connector is the fetcher, this gate is offline): PubMed MCP `convert_article_ids` (id_type='pmcid') if you have only a PMCID, then `get_article_metadata` with the PMID, and copy `identifiers`, `journal.iso_abbreviation`, `publication_date.year`, `title` and `article_types` into a record verbatim. The file's `_how_to_refresh` carries the same steps.
::error::research/autonomy/opus-capacity-campaign-20260908/reports/W09e-views-blast-radius.md:221 TYPE CLAIM WITH NO CACHED METADATA — prose calls PMID 41689087 "a case report" ("case report (PMID 41689087") and there is no row for PMID 41689087 in research/manuscripts/citation-article-types.json. ⛔ THIS IS NOT A PASS — the guard cannot check a claim it has no metadata for, so it fails. Fetch it in an interactive session (the connector is the fetcher, this gate is offline): PubMed MCP `convert_article_ids` (id_type='pmcid') if you have only a PMCID, then `get_article_metadata` with the PMID, and copy `identifiers`, `journal.iso_abbreviation`, `publication_date.year`, `title` and `article_types` into a record verbatim. The file's `_how_to_refresh` carries the same steps.
lint_citation_types: 13 type claim(s) disagree with PubMed — see errors above
