python3 set_stability.py fusion-program-testsets.json > set-stability.json

# NOTE: the command redirects stdout to the named artifact file, which IS the full stdout.
# checks/03-set-stability/stdout.txt holds only the first bytes of that stream, for identification.
