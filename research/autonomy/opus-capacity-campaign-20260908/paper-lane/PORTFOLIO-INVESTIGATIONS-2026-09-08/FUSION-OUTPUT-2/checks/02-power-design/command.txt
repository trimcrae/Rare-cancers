python3 power_design.py fusion-program-testsets.json > design-power.json

# NOTE: the command redirects stdout to the named artifact file, which IS the full stdout.
# checks/02-power-design/stdout.txt holds only the first bytes of that stream, for identification.
