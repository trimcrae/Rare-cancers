Same failed attempt as ../03-git-apply-check-failed-attempt (this directory was created by that
attempt before its redirections failed; both are preserved rather than deleted). The relative
output paths were resolved against the lane directory instead of the repo root, so the shell
aborted with exit 1 and git never ran. The successful invocation is ../04-git-apply-check (exit 0).
