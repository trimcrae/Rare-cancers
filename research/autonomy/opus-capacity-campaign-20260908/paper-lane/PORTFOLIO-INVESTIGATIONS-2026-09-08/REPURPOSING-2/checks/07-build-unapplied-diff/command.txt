diff -u <committed manuscript> <lane AFTER copy>  (AFTER copy built by an in-lane python script with 7 exact-match assertions; each anchor matched exactly once)
(exit 1 = diff found differences, which is the intended outcome)
