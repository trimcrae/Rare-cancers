--- a/research/modalities/junction_proteome_novelty.py
+++ b/research/modalities/junction_proteome_novelty.py
@@ -82,7 +82,16 @@
     "?query=%28proteome%3AUP000005640%29+AND+%28reviewed%3Afalse%29"
     "&format=fasta&includeIsoform=true&compressed=true"
 )
-PARENTS = {"P56945": "EWSR1", "Q92570": "NR4A3"}
+# ⚠ EWSR1 IS Q01844 HERE. Every other module in this repository records human EWSR1 as Q01844
+# (nr4a_paralogue_unique_residues.py, emc_fet_construct_designs.py, fusion_neoantigen.py,
+# emc-construct-inputs.json), and junction-selfsimilarity.json's own proteome hits name the
+# EWSR1 entries Q01844 / -2 / -3 / -5 / -6. With P56945 alone this map, and therefore the
+# ⛔_upstream_filter_check below, could only ever flag NR4A3: a junction peptide occurring
+# verbatim in wild-type EWSR1 or any EWSR1 isoform would be counted in n_found_in_proteome
+# but would NOT appear in parent_protein_hits and would NOT trip the BROKEN verdict.
+# P56945 is LEFT IN PLACE rather than replaced: this change can only make the guard catch
+# more, never less.
+PARENTS = {"Q01844": "EWSR1", "P56945": "EWSR1", "Q92570": "NR4A3"}
 SENTINEL = "\x00"
 
 
