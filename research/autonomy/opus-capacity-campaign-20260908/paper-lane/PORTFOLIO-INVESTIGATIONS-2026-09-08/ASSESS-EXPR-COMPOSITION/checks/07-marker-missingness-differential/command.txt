cd <assess>/work && python3 missingness.py
# Quantifies the marker missingness found in check 06: how many of the 20 markers actually
# enter the NaN-skipping composition score C per sample, whether that count differs between
# the EMC and comparator arms, and what C looks like if only fully-observed markers are used.
