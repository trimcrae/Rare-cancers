cd <assess>/work && python3 sham_matched_control.py 300
# Sham-covariate control the lane did not run: 20-gene NON-marker scores used as the
# covariate in place of C, on the primary frame, with the lane's own machinery.
# Asks whether attenuation is specific to composition markers or generic to any
# covariate that tracks the EMC label.
