mcp__PubMed__get_article_metadata(pmids=["29573200"])
# MCP tool call, not a shell command. stdout.txt holds the fields transcribed into the ledger rows,
# copied verbatim from the response; the full author list, abstract, keywords and MeSH terms were
# also returned and are summarised in FINDING.md.
