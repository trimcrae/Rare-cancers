HONESTY NOTE. This attempt was run inline in the shell immediately after the producer edits,
before this checks/ directory existed. The files here TRANSCRIBE that inline run (command as
typed, its single stdout line, its empty stderr, its shell exit status 0); they are not the
captured stream files of that run, and nothing was re-run to produce them.
