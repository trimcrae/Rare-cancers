# V1 — served-model receipt

Parent verification command run at **Tue Sep  8 11:46:51 UTC 2026** — from `date -u` in the same command, not composed. After
the V1 contract commit `0330edbe`.

| item | observed |
|---|---|
| child | `a36744029567f093b` |
| requested | `claude-opus-5`, **medium** |
| route | existing first-party saved subscription — no paid fallback, overage, credits or GPU |
| writes shared? | **none** — isolated writing lane `/tmp/claude-0/v1-lane/` only |

Parsed from transcript model fields, **not inferred from contract text**.

## What V1 is

The **R2 follow-through** under the coordinator's concrete decision: T2 adopted on the assembled
record's **case-level signal**, the **four-tier scale retained with T3 defined but unoccupied**, the
committed manuscript as baseline for an **explicitly new working revision — never a recovered
original**, and **no clinical efficacy finding**.

**No duplicate:** R2 and U2 are terminal and their outputs are **reused, not re-run** — in particular
U2's visual result, and the settled fact that the caption's *"after clinician review"* means there is
**no caption/figure mismatch**.

## The line V1 must hold

⭐ It corrects the **description** of the firewall, never the rule: **the T3 threshold and the
clinician-review requirement stay exactly as they are.** What changes is the unsupported claim that
the firewall **is fully enforced** — recast as the stated criterion, with the **imatinib registry
listing disclosed as falling below it** under the corrected grade and **registry conformance marked
unresolved**, visible **in the body**, not buried in an editorial comment. **No invented clinician
approval, exception, prospective-only policy or governance resolution**, and no registry or
patient-facing change.

Its rationale is narrowed too: **a case-level signal that does not establish T3 evidence** — not R2's
broader "neither prospective nor substantial" — because **neither R2 nor the coordinator has newly
read the case report.** Citation, counts, uncertainty and the **entire reference list** are preserved.

If the figure's labels need no change under the adopted decision, **changing nothing is the better
outcome**; any relabelling happens on a **copied** generator in the lane, and **static relabelling
validates nothing** about governance or treatment evidence.

## Live counts

**2 research children executing** — U1 (independent ATR review, expanded in flight) and V1 — plus
**1 helper** (the legacy Bash waiter, which computes nothing and is **not** research). Neither writes
anything shared; **parent integration alone owns shared files**, and final integration remains the
**scientific coordinator's** decision.
