# P1–P3 closure reconciliation — concrete disposition

Prepared by the campaign coordinator (existing parent/owner) at 2026-09-08 ~05:16Z against the retained
closure records conveyed by the standing orchestrator. **No scientific computation was repeated for this
reconciliation, no new synthesis was commissioned, and no duplicate scientific review was run.** P1–P3 had
already returned under their existing contracts; this is a reconciliation of those returned contracts, read
from their collected reports and from committed repository records.

## Note on the supplied records, stated rather than asserted

The supplied root `/tmp/claude-0/frozen-corpus/extracted/corpus/` is present and was inspected (members:
`AGENTS.md CLAUDE.md README.md research scripts systems`). **No `evidence-4878` capsule and no closure-map
member exists anywhere under `/tmp/claude-0/frozen-corpus/`** — `find -iname "*4878*"`, `-iname "*closure*"`
and `-iname "*capsule*"` return no such object (the `*closure*` hits are unrelated repository files:
`valb_triangle_closure.py` and siblings). The five `4878` hits inside the repository are a seat-regression
record and three literature probe caches, none of them a closure map. I therefore reconciled against **the
operative inventory conveyed in the orchestrator's message itself** plus the repository's own retained closure
records, and I do **not** assert having read a local watch-state object or a capsule that is not available here.

---

## P1 — `PUB-IPD-SURVIVAL`

**Exact contract as dispatched and executed:** verify each quantitative assertion in the endpoint's current
(non-superseded) record against a committed artifact, grade each PRIMARY / RETRIEVED / PREDICTION /
UNSUPPORTED, and draft the Results and Limitations that the verified inputs support. It was **not** a contract
to apply an inverse baseline, and **not** a contract touching long-term recurrence.

**Retained closure A — IPD inverse-baseline application, ended incomplete/negative after 900 s; further work
not justified absent a VERIFIED USEFUL APPLICATION; an unchanged replay is not an eligible paper step.**
- **What P1 actually ran:** `python3 research/modalities/emc_ipd_survival.py --check` → **exit 0**, after
  reading the check path in source to confirm write-freedom. Everything else was a file read. **P1 executed no
  inversion and no baseline application**; the 11-patient reconstruction it reports is read from the committed
  `emc-ipd-survival.json` (blob `23053d19…`), not recomputed.
- **Determination: UNCHANGED as to the application.** P1 supplies **no verified useful application** and claims
  none — its own headline is the opposite, that no pooled time-to-event dataset exists, carried by the artifact's
  own `pooled.⛔_this_is_not_a_pool`. **Disposition: STOP.** No inverse-baseline continuation is dispatched, and
  none of P1's output may be represented as delivering a usable pooled dataset.
- **Distinct element that is not the closed computation:** the reporting-practice census (7 curves / 0
  numbers-at-risk rows across `masunaga2025` n=171 and `chiusole2020` n=59, with per-figure verdicts and
  `pdf_sha256` committed in `km-risk-row-detection.json`, blob `0b85b367…`). That is a question about what
  journals print, not about whether an inversion is useful. It is recorded as distinct; **it is not being
  advanced further in this campaign**, and no worker is dispatched on it.

**Retained closure B — long-term recurrence failed the two-cohort / source-precision gate; reopening requires
actual complete Paioli methods/figures with endpoint and time-origin resolution.**
- **P1's contract never entered this gate**, so there is nothing to replay.
- **Evidence that the reopening input has NOT arrived**, from a committed file rather than an inventory row:
  `research/modalities/emc-site-curation.json:29` — *"Paioli 2020 (PMID 32572850, DOI
  10.1245/s10434-020-08737-7, Ann Surg Oncol), 67 molecularly confirmed localised EMC across three Italian
  Sarcoma Group centres, 1989-2016, is NOT in the clinical registry. NCBI returns no PMCID, so its site table is
  not reachable at $0. Recorded as a lead, not curated, and NOT assumed to be non-overlapping with chiusole2020
  without checking the institutions."* File evidence: sha256 `1e826ce309dc3cc9306e15c534fe421f…`, blob
  `4b79b814f5b564185cbda553bc845a2e53506d7d`.
- **No complete Paioli methods or figures are present**, no endpoint or time-origin resolution is available, and
  **no title, abstract or graph label was accepted as a substitute**. **Disposition: STOP; gate not reopened.**

**Continue/stop: STOP.** P1's work is complete and collected; no further P1 step is dispatched.

---

## P2 — `PUB-CARE-DELIVERY`

**Exact contract as dispatched and executed:** verify the four named extraction artifacts, settle whether the
554-record corpus behind `BLK-NO-FIELD-ATTENTION-MEASUREMENT` is reachable **with no network call**, and draft
what the verified inputs support.

**Retained closure — four source-gate NO-GOs (function / referral / salvage / access), whose candidate-specific
reopening inputs are a consecutive functional denominator, initial-operation chronology, a relapse-inception
roster, or a dated operational access denominator.**
- **Which exact input belongs to which question, preserved and kept apart:** those four inputs belong to the
  function / referral / salvage / access questions. **P2's contract addressed a different gate** — the
  field-attention term census — whose own reopening input is `refs/heads/literature-cache` →
  `literature/emc-care-delivery-and-classification/` (554 records). **P2 supplied none of the four NO-GO inputs
  and claimed none**, so those four gates are neither replayed nor reopened. **Disposition for all four: STOP,
  unchanged.**
- **P2's own gate also failed, with the condition preserved exactly:** no local ref, no local object, no
  working-tree path and no frozen-corpus member resolves that path; the clone is shallow (**44** grafts);
  `literature-cache` was never fetched here; the two reflog hits for the string are this campaign's own commit
  messages. Per §2 that is **UNKNOWN about the remote, not absent** — and nothing was inferred from the absence,
  no substitute corpus was used.
- **Determination: UNCHANGED.** No changed input is claimed. **Disposition: STOP; documented.**
- **No admission was inferred from an inventory.** P2's deliverable is a verification plus a source-dependency
  statement, and its own verdict is **NOT WRITABLE** — additionally, and independently, the publish decision for
  this candidate is recorded ANSWERED-NO in ledger rows AUT-042 / AUT-057 / AUT-064. A source-dependency
  inventory is explicitly **not** treated here as paper admission.

**Continue/stop: STOP.** Complete and collected; no further P2 step is dispatched, and no route around the
recorded "no" was taken.

---

## P3 — `PUB-LOCOREGIONAL`

**Exact contract as dispatched and executed:** reproduce the two pooled proportions from the committed registry;
check the three stated negatives individually against the registry schema; **specify** (not perform) the single
curation step; draft the Results and Limitations.

**Retained closure — the locoregional/RT synthesis failed clinical alignment; Novak2026 n=7 supplies no RT
cross-tab or comparator; the prior RT citation-mismatch audit found no consequential paper merit and must not be
repeated or rebranded as an eligible manuscript because the graph says `outlined`.**
- **RT-synthesis / RT-comparator arm — UNCHANGED → STOP.** P3 performed **no RT synthesis**, used **no
  Novak2026** (no such record is present in the tree; the only `novak` string is an author field in
  `geo-gse28866-brunner-series.json`), produced **no RT cross-tab and no comparator**, and did **not** repeat the
  citation-mismatch audit. It explicitly honoured the two route-level rulings already recorded —
  `RT-RT-INTENSIFY` REFUTED (already written up as `emc-radioresistance-reappraisal.md`) and
  `RT-METASTASECTOMY` DO-NOT-WRITE — rather than reopening either. **No RT efficacy, alignment or comparator
  claim appears in its draft, and none may be added.**
- **Distinct arm, with an actual changed input and its file evidence — CONTINUE PERMITTED.** The question P3
  answered is an **anatomical-site eligibility denominator**, not an RT synthesis. The changed input is
  `research/modalities/emc-site-curation.json` (curation 2026-08-25/27), **sha256
  `1e826ce309dc3cc9306e15c534fe421f…`, blob `4b79b814f5b564185cbda553bc845a2e53506d7d`**, together with
  `research/modalities/emc_locoregional_eligibility.py:52-56`, which sets
  `primary_anatomical_site_distribution.computable = True` and carries in-source the marker
  *"⚠ *Superseded, retained: False.*"* with `"⭐ RESOLVED ELSEWHERE 2026-08-25"` naming that artifact.
  **This is file-and-hash evidence of an actual change of state, not an inventory row and not an inference from
  a missing file.** The quantity it yields (extremity fraction, 71.6% strict / 84.5% inclusive over n=271) exists
  in no prose anywhere in the repository.
- **The distinct arm's own honest limit, preserved:** the lung-directed numerator is still absent — the two
  exclusive partitions are upper bounds, one series' rows are non-exclusive, and drilon2008 is percentage-only,
  which §2.1(2) forbids converting. The specified curation step was **not performed**.

**Continue/stop: CONTINUE PERMITTED for the site-denominator arm only, STOP for the RT-synthesis arm.** P3's
contracted work is complete and collected; **no further worker is being dispatched**, because the remaining step
on the permitted arm is authorship by a named owner with write authority, not another read-only audit.

---

## Route selection after this reconciliation

**No new distinct route is selected, and none is manufactured.** The eligible candidates each already have a
completed worker (P1, P3, P4, P5, P6) or are closed (P2 / PUB-CARE-DELIVERY, publish decision ANSWERED-NO;
PUB-PARKED-MODALITIES, both capability blockers unretired). Selecting a further route would require inventing
eligibility, which the standing prioritization forbids. Worker count follows eligible work, not the reverse.

**Unchanged and untouched by this reconciliation:** the acknowledged writer / W25 primary-reading continuation
and the restricted NR4A Perspective remain EXCLUDED — no retry, reword, reroute, or admission by proxy; the
blocked writer was not prompted. Deadline 2026-09-09T02:37:19Z, Opus 5 only on the first-party subscription, no
paid fallback, source closures, scientific-integrity restrictions and integration ownership all stand. No
frozen-paper edit, no merge, no publication, no new controller or session.
