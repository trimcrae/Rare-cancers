<!-- collected by the OPUS-CAPACITY-CAMPAIGN-20260908 parent collector at 2026-09-08T03:20:24Z UTC
     agent id aa8b7c37ffc0028e2 ; transcript /root/.claude/projects/-home-user-Rare-cancers/8ecd0f49-96ba-5dcf-b11a-af5e48bdec71/subagents/agent-aa8b7c37ffc0028e2.jsonl
     OBSERVED runtime model set in that transcript: ["claude-opus-5"] (asserted exactly [claude-opus-5])
     the worker was read-only on the tree; this file is the parent's write -->

## Worker

**W24b**, repair-adjudication lane (make the `emc_fourth_cohort_quant.py` either/or decidable), OPUS-CAPACITY-CAMPAIGN-20260908. **I do not rule; I price.**

**Model identity — SELF-REPORT, NOT INDEPENDENTLY VERIFIED.** I report myself as Claude Opus 5 (`claude-opus-5`) from my own system context. No environment variable in this container names a served model; the coordinator must extract the actual per-child runtime model from the transcript.

`date -u` **start: `Tue Sep  8 03:13:03 UTC 2026`** · `date -u` **end: `Tue Sep  8 03:16:21 UTC 2026`**

`git rev-parse HEAD` **start: `98a0833f1ede2cf9cef13790388260567a32e868`** · **end: `c81236b90c7905caa2e75a57b332ad33f2c10d02`**. **HEAD moved under me** — the coordinator committed the eight reports that were untracked at my start.

`git status --porcelain` **at start** (8 lines, all untracked coordinator-staged reports, none mine):
```
?? research/autonomy/opus-capacity-campaign-20260908/reports/W01g-transcriptome-wide-criterion.md
?? research/autonomy/opus-capacity-campaign-20260908/reports/W02f-fibro-ecm-decomposition.md
?? research/autonomy/opus-capacity-campaign-20260908/reports/W04e-delay-attribution-route.md
?? research/autonomy/opus-capacity-campaign-20260908/reports/W06f-coherent-share-readout.md
?? research/autonomy/opus-capacity-campaign-20260908/reports/W07f-emc-only-cohort-full-texts.md
?? research/autonomy/opus-capacity-campaign-20260908/reports/W09f-publications-false-absence-audit.md
?? research/autonomy/opus-capacity-campaign-20260908/reports/W12e-subprocess-env-residual.md
?? research/autonomy/opus-capacity-campaign-20260908/reports/W20g-published-denominator-consistency.md
```
`git status --porcelain` **at end: empty (0 lines)** — those eight are now committed at `c81236b9`. **Zero files created or modified under `/home/user/Rare-cancers` by me**; no git write operation; no network.

Literal `env | grep -i -E 'claude|anthropic|model' | sed -E 's/(TOKEN|KEY|SECRET)[^=]*=.*/\1=<redacted>/I'` at start (51 lines; identical line count and content at end):

```
CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1
CLAUDE_CODE_ACCOUNT_UUID=4c503081-e4c3-45a1-8002-1cb705f46605
CLAUDE_CODE_CHILD_SESSION=1
no_proxy=localhost,127.0.0.1,::1,...,api.anthropic.com,...   [proxy exclusion list; matched on substring "anthropic"]
AI_AGENT=claude-code_2-1-263_agent
CLAUDE_CODE_USER_EMAIL=trimcrae@gmail.com
CLAUDE_CODE_SESSION_ID=8ecd0f49-96ba-5dcf-b11a-af5e48bdec71
CLAUDE_CODE_DEBUG=true
CLAUDE_PID=522
CLAUDE_AUTO_BACKGROUND_TASKS=true
CLAUDE_AFTER_LAST_COMPACT=true
CLAUDE_EFFORT=medium
CLAUDE_CODE_GZIP_REQUEST_BODIES=1
CLAUDE_CODE_PROVIDER_MANAGED_BY_HOST=1
CLAUDE_CODE_MESSAGING_SOCKET=/tmp/cc-socks/522.sock
CLAUDE_CODE_CONTAINER_ID=container_0166QEHnXrRA8nCR59c9UG4k--claude_code_remote--71b0d4
CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=80
CLAUDECODE=1
SESSION_INGRESS_URL=https://api.anthropic.com
CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default
CLAUDE_CODE_WORKER_EPOCH=1
CLAUDE_CODE_REMOTE_SESSION_ID=cse_01Eui7FVgatEXAwt2N35yHH6
CLAUDE_CODE_PROXY_RESOLVES_HOSTS=true
CLAUDE_CODE_DISABLE_TERMINAL_TITLE=1
GLOBAL_AGENT_NO_PROXY=...   [proxy exclusion list]
ANTHROPIC_BASE_URL=https://api.anthropic.com
CLAUDE_CODE_DIAGNOSTICS_FILE=/tmp/claude-code-303934770.diag.log
CLAUDE_ENABLE_STREAM_WATCHDOG=1
CLAUDE_CODE_REMOTE_HERMETIC_MODE=0
CLAUDE_CODE_ENVIRONMENT_RUNNER_VERSION=release-ba76006550-ext
CLAUDE_CODE_DISABLE_BUILTIN_ANTMCP=1
CLAUDE_ADDITIONAL_DIRECTORIES=/mnt/user-data
JAVA_TOOL_OPTIONS=...   [proxy exclusion list]
NO_PROXY=...            [proxy exclusion list]
CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH=1
CLAUDE_CODE_USE_CCR_V2=true
CLAUDE_CODE_SYNC_SESSION_REFS=1
DOCUMENTS_MCP_SCRATCH_ROOT=/mnt/user-data/working/claude-docs
CLAUDE_CODE_TEE_SDK_STDOUT=true
CLAUDE_CODE_ENTRYPOINT=remote
CLAUDE_CODE_VERSION=2.1.42
CLAUDE_CODE_EXECPATH=/opt/claude-code/bin/claude
npm_config_noproxy=...   [proxy exclusion list]
CLAUDE_CODE_REMOTE_SEND_KEEPALIVES=true
CLAUDE_CODE_REMOTE=true
CLAUDE_CODE_POST_FOR_SESSION_INGRESS_V2=true
CLAUDE_CODE_ORGANIZATION_UUID=9f10e945-e412-4b71-8a9d-5d6affcd7d4f
CLAUDE_SESSION_INGRESS_TOKEN=<redacted>
CLAUDE_CODE_SYNC_SKILLS=1
CLAUDE_CODE_HOLD_UNANSWERED_PARKED_PERMISSION=1
CLAUDE_CODE_MESSAGING_TOKEN=<redacted>
```

**Where I executed `--check`:** on the **scratch copy only**, at `/tmp/claude-0/w24b/research/modalities/`, made with `tar --exclude=./.git`. I did **not** run it against the live tree. All my execution was under `/tmp/claude-0/w24b/`.

---

## Stop condition (set up front)

**Stop when the comparison table is complete: for each of R07, R08 and the W20g third candidate (C3) — the exact committed lines it changes, every published count and hash it moves *recomputed from the committed TSVs on my scratch copy* rather than asserted, the named downstream consumers of each moved value, and what it does not fix — plus a reasoned determination of whether the either/or is real. Return the moment that is complete; do not rule, do not author either repair.** — **MET.** Returning immediately.

---

## Question

W24 flagged R07 (from W14c) and R08 (from W14d) as two repairs for one target and called it a **true either/or**. Is it? And what would an owner have to know to choose in one sitting rather than re-deriving both? W20g additionally names a **third** candidate touching the same file. The open question is not "which repair is right" — that is the module owner's — but **"is the choice actually forced, and what does each option cost in committed bytes and downstream pins."**

---

## Prior-work check

| Command (all read-only) | What it showed |
|---|---|
| `grep -n "R07\|R08\|either/or\|EITHER" reports/W24-repair-routing-index.md` | Rows 137–139: the flag, its discriminating facts, and D01 (the owner's decision row) at `:170`. |
| Read in full: `COMMON-BRIEF.md`, `CLOSED-WORK.md`, `CORPUS-CONTEXT.md`, `reports/W24-*` (R07/R08 rows + §stop), `reports/W14c-fake-guard-repair-proof.md`, `reports/W14d-fourth-cohort-guard-repair.md`, `reports/W20g-published-denominator-consistency.md`, `reports/W20f-probe-intersection-filter.md` §6 | Both repairs in full, with their diffs and acceptance runs. W20g's §"Next concrete action" is the C3 candidate. |
| `grep -rln "c689e0fd…" --exclude-dir=.git .` / `grep -rln "1769b3f0…"` | Exactly who carries the committed probe-TSV hash and the artifact file hash (below). |
| `grep -rn "probe_counts_sha256\|assigned_gene\|emc-fourth-cohort-probe-counts" --exclude-dir=.git .` | The four real downstream consumers plus the CI workflow. |
| `grep -n "expression_validation_readiness\|fourth_cohort" scripts/preflight.sh` → **exit 1, no match** | Independently confirms W14d §5: **zero preflight rows touch this file.** |

**I am replaying nothing closed.** I drew no EMC scientific conclusion, re-derived no panel, re-tested no endpoint (W20d/W20e stay closed), touched no `systems/graph/` file, made **no network request** — the Ensembl fetch (`:409`) and the vendor TempO-Seq manifest remain honest unrecovered sources and were neither attempted nor routed around. No content-policy refusal was encountered. I did not author or land any repair.

---

## Method and inputs

- Live checkout `/home/user/Rare-cancers`, read-only, HEAD `98a0833f…` → `c81236b9…`.
- **Scratch copy** at `/tmp/claude-0/w24b/`, made with `tar --exclude=./.git -cf - . | (cd /tmp/claude-0/w24b && tar -xf -)` (611 MB). All execution there.
- Target file `research/modalities/emc_fourth_cohort_quant.py`, **958 lines**, sha256 `c5180c11a57463337d023fda58fb5516833d1eb4e4787095e15c35af5eb5965f` — **identical to the sha W14d read at `3f5fc95d…`**, so neither repair has landed and the file has not moved since either was cut.
- Committed artifacts read: `emc-fourth-cohort-quant.json`, `-inputs.json`, `-probe-counts.tsv` (213,007 rows × 12), `-gene-counts.tsv` (862 × 12).
- Recomputation script `/tmp/claude-0/w24b/_w24b_recompute.py`, CPython **3.11.15** (`/usr/local/bin/python3`), stdlib only (`json`, `hashlib`, `collections`). No network, no GPU, no paid API.

---

## Result

### 0. The three candidates, anchored to committed line numbers

Anchors verified by `grep -n` on the committed file (all `PRIMARY`):

```
570:def _write_probe_tsv(cache: dict, gene_of=None) -> str:
586:        g = (gene_of or {}).get(pr) or "unassigned"
590:    tmp = PROBE_TSV + ".tmp"
593:    os.replace(tmp, PROBE_TSV)
739:def derive(cache: dict | None = None) -> dict:
817:        body = _write_probe_tsv(cache, p2g)
830:        with open(GENE_TSV, "w", encoding="utf-8") as fh:
933:    out = derive()
934:    if args.check:
940-941:  drift = [k for k in ("probe_gate", "n_runs_read", "n_probes_common_to_every_read_run",
                              "n_genes_with_at_least_one_assigned_probe", "gene_counts_sha256")
```

| | **R07** (W14c) | **R08** (W14d) | **C3** (W20g's successor, unauthored) |
|---|---|---|---|
| Lines changed | **570, 586–593, 739, 817, 830–831, 933–943** (6 hunks) | **570, 589–593, 739, 817, 830–831, 933–943** (4 edits, 92 diff lines) | **586** (the label expression) + one new field in the `out` dict near **820** |
| Shape | Split `_write_probe_tsv` → `_probe_tsv_body_impl` / `_probe_tsv_body` / writer; `derive(*, write=True, bodies=None)`; `--check` compares full bodies carried out via `bodies` | `write=` kwarg on `_write_probe_tsv` and `derive`; `--check` sha256-hashes the committed bytes **off disk before `derive()` runs**, compares against `out["*_sha256"]` | Replace the single `"unassigned"` with the three states already computed (`unassigned` / `multi_gene` / `not_offered`), plus one artifact disclosure field |
| Adds `probe_counts_sha256` to the drift-key tuple at 940–941 | **Yes** | **Yes** | No |
| What it is *for* | make the guard capable of failing | make the guard capable of failing | make one **label** honest |

### 1. What each moves in `emc-fourth-cohort-quant.json` — **recomputed, not asserted**

Baseline, recomputed from the committed TSV bytes on the scratch copy (`PRIMARY`, RUN, exit 0):

```
COMMITTED probe tsv sha256      : c689e0fd4f5c8cda0f132cd8ce42f039f691bf94cb8d81d3f622886e2bdd26fc
  JSON probe_counts_sha256      : c689e0fd4f5c8cda0f132cd8ce42f039f691bf94cb8d81d3f622886e2bdd26fc   [agree]
COMMITTED gene tsv sha256       : 8aa3064a97a496a8fa09ef4ae8e472a612318e49ea91d0daac915a3b8fa180cc
  JSON gene_counts_sha256       : 8aa3064a97a496a8fa09ef4ae8e472a612318e49ea91d0daac915a3b8fa180cc   [agree]
  JSON artifact file sha256     : 1769b3f0ee01f1a640dabc6c4f36ce86630ff62f87249400e070a4638fcf5165
n_rows 213007  (JSON probe_counts_n_rows 213007)  run cols 12
offered (k=12) 1645 ; rows labelled with a gene 906 ; rows labelled "unassigned" 212101
```

**R07 and R08 move nothing.** Verified independently rather than taken from the two reports: on the scratch copy I ran the **unrepaired** generator's `--check`, which calls `derive()` and therefore **rewrites both TSVs from the inputs**, and both came back bit-identical:

```
$ cd /tmp/claude-0/w24b/research/modalities && python3 emc_fourth_cohort_quant.py --check
no drift: the derive half reproduces offline
EXIT=0
c689e0fd…d26fc  emc-fourth-cohort-probe-counts.tsv     <- unchanged
8aa3064a…180cc  emc-fourth-cohort-gene-counts.tsv      <- unchanged
1769b3f0…5165   emc-fourth-cohort-quant.json           <- unchanged (--check never writes ART)
```

The write path reproduces the committed bytes exactly. Since R07 and R08 both leave the *derivation* untouched and only change *when and against what* the comparison happens, **no published count, no hash, and no TSV byte moves under either.** That is a computed confirmation of both reports' T5/T6 claims, from a third script.

**C3 moves exactly one published hash, and it is a large byte move.** Recomputed by relabelling the committed probe TSV using `probe_to_gene` (906) / `probe_to_several_genes` (77) from `emc-fourth-cohort-quant-inputs.json` and the k=12 offered set (1,645), and rehashing (`PRIMARY`, RUN, exit 0):

```
C3 label distribution           : {'<gene>': 906, 'unassigned': 662, 'multi_gene': 77, 'not_offered': 211362}
C3 probe tsv sha256             : 6730b5a0662c28eaf29324c9fbe37249d155c34cc01f2408f3eeb243f39c86fd
C3 n_rows                       : 213007          (unchanged)
C3 byte size committed -> new   : 18,810,774 -> 19,022,136   (exactly +211,362 bytes)
C3 gene tsv sha256              : 8aa3064a…180cc  (unchanged)
```

The +211,362 bytes is exactly one character per `not_offered` row (`unassigned` 10 chars → `not_offered` 11; `multi_gene` is also 10, so it contributes zero).

| Published value | R07 | R08 | **C3** |
|---|---|---|---|
| `probe_counts_sha256` | `c689e0fd…` → **unchanged** | `c689e0fd…` → **unchanged** | `c689e0fd…` → **`6730b5a0662c28eaf29324c9fbe37249d155c34cc01f2408f3eeb243f39c86fd`** (computed) |
| `gene_counts_sha256` | unchanged | unchanged | unchanged (`8aa3064a…`) |
| `probe_counts_n_rows` = 213,007 | unchanged | unchanged | unchanged |
| `gene_counts_n_rows` = 862 | unchanged | unchanged | unchanged |
| `n_probes_offered` / `n_probes_in_every_read_run` / `n_probes_common_to_every_read_run` = 1,645 | unchanged | unchanged | unchanged |
| `n_probes_assigned_to_one_gene` 906 / `…several_genes` 77 / `n_probes_unassigned` 662 | unchanged | unchanged | unchanged |
| `n_genes_with_at_least_one_assigned_probe` = 862 | unchanged | unchanged | unchanged |
| **Artifact file sha256** `1769b3f0…` | unchanged | unchanged | **changes** — value **not computable to a fixed string**, because C3 also adds a disclosure field *and* `_generated_utc` is a wall-clock stamp regenerated on every write. Honest answer: **UNKNOWN until written**, and non-deterministic by construction. |
| Probe TSV bytes on disk | unchanged | unchanged | **+211,362 bytes**, 211,362 rows relabelled |

**Correction to W20g, on computed evidence.** W20g's successor note states C3 leaves "the 906 labelled rows, the 862 genes, **both TSV hashes** and every gene count … untouched". The 906/862/gene-count half is confirmed. **The "both TSV hashes" half is wrong: `probe_counts_sha256` moves**, `c689e0fd…` → `6730b5a0…`, and the artifact file hash moves with it. C3 is count-neutral, not hash-neutral. That distinction is the whole of C3's downstream cost.

### 2. Downstream consumers of each moved value (`PRIMARY`, by exhaustive grep)

Carriers of the committed probe-TSV hash `c689e0fd…`, excluding campaign reports and my own scratch copies: **`research/modalities/emc-fourth-cohort-quant.json`** and **`research/modalities/expression-validation-readiness.json`** — nowhere else in the tree.

Carriers of the artifact file hash `1769b3f0…`: `expression-validation-readiness.json` (**64 occurrences**, one `file_sha256` pin per `quant#/…` RetainedFact — 128 `quant#/` references in total), plus three historical `research/autonomy/cycle-outcomes/20260905T111908Z-083a73575a/` records (`plan.json`, `cycle.json`, `original-collected-cycle.json`).

| Moved value | Consumer | How it reads it | Effect |
|---|---|---|---|
| *(R07 / R08 move nothing)* | — | — | **No consumer of any value is affected by either.** Their only observable change is `--check`'s **exit code**. |
| `--check` exit code | `.github/workflows/emc-expression-datasets.yml:1065-1066` | `python … --check \|\| echo "--check reported drift; non-blocking, but READ IT"` — **explicitly non-blocking** | Repairing the guard does **not** enforce it. `grep -n "fourth_cohort" scripts/preflight.sh` → **no match, exit 1**: zero preflight rows. Independently confirmed. |
| `probe_counts_sha256` (C3) | `research/modalities/expression_validation_readiness.py:257` → `expression-validation-readiness.json:12635-12641`, RetainedFact `quant#/probe_counts_sha256`, `value: "c689e0fd…"` | pinned value + `value_sha256` `1486702196e7…` | Recorded value becomes **stale and wrong about HEAD**. |
| Artifact file sha256 (C3) | `expression-validation-readiness.json`, **64 `file_sha256` pins** | provenance chain for every `quant#/…` fact | All 64 pins become stale. |
| **The staleness is SILENT — this is the load-bearing consumer finding.** `expression_validation_readiness.py:17-18,55` reads its inputs via `subprocess.check_output(["git", "show", f"{BASE}:{path}"])` with **`BASE = "8c1f292536b7d725186491b42c19c87c0b6c855c"`**, a frozen revision — not the working tree. So after C3, `expression_validation_readiness.py --check` **still passes**, while the readiness file publishes a `probe_counts_sha256` that no longer describes the committed table. There is no guard anywhere that would catch this. | | | |
| Probe TSV bytes / `assigned_gene` column (C3) | `emc_fourth_cohort_quant.py:552-566` `load_inputs()` | reads `parts[0]` (sequence) and `parts[2:]` (counts) only — **the label column is never read back** | **No rehydration breakage.** C3 is safe for the checkpoint round-trip. Verified by code read. |
| Probe TSV as shipped artifact (C3) | `.github/workflows/emc-expression-datasets.yml` (uploaded standalone) | consumers computing an "unassigned fraction" | 99.57 % → 0.31 % for the same phrase. This is the defect C3 exists to fix. |
| `n_probes_unassigned` (662), `gene_counts_sha256` | `research/modalities/emc_fourth_cohort_route_readout.py:163-167` | reads `n_probes_unassigned` and recomputes the **gene** TSV sha | **Untouched by all three.** The route readout JSON contains **zero** occurrences of `probe_counts_sha256`. |
| `systems/graph/routes.json:7235`, `systems/views/L2-rt-immunocytokine.md:69` | prose citing "the committed probe table carries `probe_sequence` and `assigned_gene`" | **Untouched by R07/R08.** Under C3 the column gains two new values, which makes that sentence more informative and not less true; no number in it moves. |

### 3. What each does **not** fix

| | Does not fix |
|---|---|
| **R07** | (a) The workflow row stays **non-blocking** — the guard can fail and CI still passes; W14c says so itself and left it. (b) No preflight row is added. (c) The `unassigned` label still conflates 662 + 77 + 211,362 (W20g §3). (d) The k=12 intersection filter still withholds 211,362 of 213,007 sequences from the matcher (W20f). (e) `_generated_utc` remains a wall-clock field inside the artifact. (f) **R07 never ran a probe-*count* drift test (W14d's D3).** Its diff *does* add `probe_counts_sha256` to the key tuple, so on reading it should catch D3 — but that is **PROPOSED (NOT RUN); I did not execute it and neither did W14c.** |
| **R08** | (a)–(e) identical to R07. R08 additionally leaves `_write_probe_tsv(..., write=False)` as a function named for a write that it does not perform — a naming cost, not a correctness one. |
| **C3** | Does **not** touch the guard at all. After C3, `--check` is still incapable of failing on either TSV: it would rewrite the relabelled table and agree with itself. Worse, **C3 is exactly the class of change the fake guard hides** — an author who lands C3 and forgets to regenerate the committed TSV gets `no drift … exit 0` in CI. C3 also does not restore any withheld sequence: the 211,362 stay unoffered, `not_offered` merely says so out loud. And it does not resolve the TNC/FN1 route question, which needs the denied Ensembl fetch and stays **UNKNOWN**. |

### 4. Is the either/or real? **Yes for R07 vs R08. No for C3.**

**R07 vs R08 — the exclusion is real at both levels (`PRIMARY`, by code reading; the mechanical failure is `PROPOSED (NOT RUN)` — I did not run `git apply`, being read-only and forbidden to author either repair).**

- *Textually:* the two rewrite the **same** `--check` branch (933–943) into two different bodies, and both rewrite lines 817 and 830–831. R08's hunk context at 817 (`body = _write_probe_tsv(cache, p2g)`) **ceases to exist** once R07 lands (it becomes a conditional expression); R07's hunk at 570 conflicts with R08's kwarg on the same `def`. Whichever lands second cannot apply.
- *Semantically:* they implement the **same guarantee** — compare the committed bytes, never the bytes just written — by two mechanisms. Layering them would give `derive(*, write, bodies)` with the `bodies` path and the off-disk-sha path both live: two comparisons of the same fact, one of which carries an 18.8 MB body through the call stack for nothing. That is redundancy, not defence in depth.
- **Is there any input state under which both apply?** No. Their trigger condition is identical (`args.check` with `read_runs` non-empty) and their outputs on all four drift classes are the same verdict by different routes. There is no input on which one fires and the other does not.
- **Does one strictly foreclose the other?** Yes, and symmetrically: this is a genuine choice between two designs, not a sequence. W24's read is correct.
- **The discriminator W24 records is weaker than it looks.** W24 row 139(b) credits W14d with finding D3, "*fixed only by adding `probe_counts_sha256` to the drift key tuple*". **R07's diff adds that same key** (`"gene_counts_sha256", "probe_counts_sha256"` in its rewritten tuple). So the capability is in both patches. What is in **only R08** is the **executed D3 test** (`DRIFT: ['probe_counts_sha256']`, exit 1, corruption left on disk). The honest discriminator is therefore **test coverage — 8 executed acceptance rows on 3 drift classes vs 7 rows on 2 — plus size, not capability.**

**C3 vs both — not an either/or, and not a substitute either way.**

- C3 touches **line 586 only** (plus a new `out` field). R07 rewrites 586–593 as a block move but does not change that expression; R08 does not touch 586 at all. So **C3 conflicts textually with R07's hunk and not with R08's** — a real, if minor, ordering fact for the owner. Semantically C3 is orthogonal to both.
- **Does C3 make R07 or R08 unnecessary? Not at all — and it strictly increases the case for one of them.** They answer disjoint questions: R07/R08 ask *can this guard fail?*; C3 asks *does this label tell the truth?* C3 is the first change in this cluster that actually **moves a committed hash**, which is precisely the event a working guard exists to catch and the current guard silently launders.
- Direction of dependency: **C3 is safer to land after R07 or R08, and either is safe to land alone.** With the guard repaired, a C3 landing that forgets to regenerate the committed TSV produces `DRIFT: ['emc-fourth-cohort-probe-counts.tsv']` exit 1 (non-blocking in CI, but visible). With the guard as it stands, it produces `no drift … exit 0`.

### 5. The owner's ruling table

Everything below is measured or read from committed source. **I do not recommend a row.**

| Axis | **R07** (W14c) | **R08** (W14d) | **C3** (W20g, unauthored) |
|---|---|---|---|
| Lines touched | 570, 586–593, 739, 817, 830–831, 933–943 | 570, 589–593, 739, 817, 830–831, 933–943 | 586 + one new `out` field |
| New functions / params | +2 functions, +2 kwargs (`write`, `bodies`) | +1 kwarg (`write`) on two defs | none |
| Committed counts moved | **0** (recomputed) | **0** (recomputed) | **0** (recomputed) |
| Committed hashes moved | **0** | **0** | **1** — `probe_counts_sha256` `c689e0fd…` → **`6730b5a0…`** (computed); artifact file sha also moves, value **UNKNOWN**/non-deterministic |
| Committed bytes moved | 0 | 0 | **+211,362** in the probe TSV |
| Downstream pins invalidated | 0 | 0 | 1 RetainedFact value + **64** `file_sha256` pins in `expression-validation-readiness.json` — **and they fail SILENTLY** (that generator reads `git show BASE:…`, `BASE=8c1f2925…`, so its `--check` still passes) |
| Rehydration (`load_inputs`) risk | none | none | **none** — the label column is never read back (`:552-566`) |
| Executed acceptance rows | **7** (T1–T7), 2 drift classes | **8** (T1–T8), **3** drift classes incl. probe-count D3 | **0 — nothing authored, nothing run** |
| D3 (probe-count drift) capability | in the diff (`probe_counts_sha256` added) but **untested — PROPOSED (NOT RUN)** | **tested**: `DRIFT: ['probe_counts_sha256']`, exit 1, corruption left | n/a |
| Independent replication of the defect | first finder | **replicates W14c's D1/D2 with identical intermediate shas** | replicates W20f's k-histogram independently |
| Memory cost at `--check` | carries both bodies (~18.8 MB) out of `derive()` | sha256 of on-disk bytes; no body carried | n/a |
| Naming cost | none | `_write_probe_tsv(write=False)` writes nothing | none |
| Preflight rows affected | **0 of 18** (confirmed: no match in `scripts/preflight.sh`) | **0 of 18** | **0 of 18** |
| CI enforcement after landing | still non-blocking (`\|\| echo`, workflow `:1065-1066`) | still non-blocking | n/a |
| Mutually exclusive with | **R08** (hard) ; conflicts textually with **C3** at 586 | **R07** (hard) ; no conflict with C3 | conflicts with R07's hunk; also overlaps **W20f's** repair, which rewrites 584–588 and is **network-blocked** |
| Fixes the `unassigned` conflation | no | no | **yes** — 662 / 77 / 211,362 stated |
| Fixes the k=12 withholding | no | no | no (W20f's repair, blocked) |

---

## Validation evidence

Environment for every run: CPython **3.11.15** (`/usr/local/bin/python3`, `[GCC 13.3.0]`), Linux 6.18.44-fc-v24, container `container_0166QEHnXrRA8nCR59c9UG4k--claude_code_remote--71b0d4`, **no network**, cwd under `/tmp/claude-0/w24b/`.

**RUN — scratch copy created (no `.git`):**
```
$ mkdir -p /tmp/claude-0/w24b && tar --exclude=./.git -cf - . | (cd /tmp/claude-0/w24b && tar -xf -)
COPIED     611M	/tmp/claude-0/w24b
```

**RUN — `--check` on the SCRATCH COPY ONLY (exit code real, TSVs rewritten there, not in the tree):**
```
$ cd /tmp/claude-0/w24b/research/modalities && python3 emc_fourth_cohort_quant.py --check
no drift: the derive half reproduces offline
EXIT=0
$ sha256sum emc-fourth-cohort-probe-counts.tsv emc-fourth-cohort-gene-counts.tsv emc-fourth-cohort-quant.json
c689e0fd4f5c8cda0f132cd8ce42f039f691bf94cb8d81d3f622886e2bdd26fc  emc-fourth-cohort-probe-counts.tsv
8aa3064a97a496a8fa09ef4ae8e472a612318e49ea91d0daac915a3b8fa180cc  emc-fourth-cohort-gene-counts.tsv
1769b3f0ee01f1a640dabc6c4f36ce86630ff62f87249400e070a4638fcf5165  emc-fourth-cohort-quant.json
$ git -C /home/user/Rare-cancers status --porcelain   # unchanged, only the coordinator's 8 untracked reports
```

**RUN — recomputation (`/tmp/claude-0/w24b/_w24b_recompute.py`), verbatim:**
```
COMMITTED probe tsv sha256      : c689e0fd4f5c8cda0f132cd8ce42f039f691bf94cb8d81d3f622886e2bdd26fc
  JSON probe_counts_sha256      : c689e0fd4f5c8cda0f132cd8ce42f039f691bf94cb8d81d3f622886e2bdd26fc
COMMITTED gene tsv sha256       : 8aa3064a97a496a8fa09ef4ae8e472a612318e49ea91d0daac915a3b8fa180cc
  JSON gene_counts_sha256       : 8aa3064a97a496a8fa09ef4ae8e472a612318e49ea91d0daac915a3b8fa180cc
  JSON artifact file sha256     : 1769b3f0ee01f1a640dabc6c4f36ce86630ff62f87249400e070a4638fcf5165
n_rows                          : 213007 JSON probe_counts_n_rows 213007 run cols 12
offered (k=12)                  : 1645
committed label counts          : [('unassigned', 212101)] labelled non-unassigned: 906
C3 label distribution           : {'not_offered': 211362, '<gene>': 906, 'unassigned': 662, 'multi_gene': 77}
C3 probe tsv sha256             : 6730b5a0662c28eaf29324c9fbe37249d155c34cc01f2408f3eeb243f39c86fd
C3 n_rows                       : 213007
C3 byte size committed->new     : 18810774 -> 19022136
C3 gene tsv sha256 (unchanged)  : 8aa3064a97a496a8fa09ef4ae8e472a612318e49ea91d0daac915a3b8fa180cc
EXIT=0
```
(These reproduce W20g's 662 / 77 / 211,362 decomposition from an independently written script — an incidental second replication.)

**RUN — consumer enumeration:**
```
$ grep -n "expression_validation_readiness\|fourth_cohort" scripts/preflight.sh
(no output)   EXIT=1        <- zero preflight rows, confirming W14d §5 independently

$ python3 -c "...expression-validation-readiness.json..."
count of "quant#/" occurrences: 128
count of 1769b3f0… occurrences: 64
count of c689e0fd… occurrences: 2

$ grep -n "^BASE" research/modalities/expression_validation_readiness.py
18:BASE = "8c1f292536b7d725186491b42c19c87c0b6c855c"
$ sed -n '55p' research/modalities/expression_validation_readiness.py
            raw = subprocess.check_output(["git", "show", f"{BASE}:{path}"], cwd=ROOT)

$ python3 -c "...emc-fourth-cohort-route-readout.json..."
probe_counts_sha256 0 ; gene_counts_sha256 1 ; n_probes_unassigned 1
```

**RUN — file identity:**
```
$ sha256sum emc_fourth_cohort_quant.py ; wc -l emc_fourth_cohort_quant.py
c5180c11a57463337d023fda58fb5516833d1eb4e4787095e15c35af5eb5965f
958
```

**PROPOSED (NOT RUN), explicitly:**
- `git apply --check` of either repair, and any application of R07, R08 or C3 to any file. **I authored no repair and applied none**, per my brief. The textual-conflict finding in §4 is from reading both diffs against the committed line anchors, not from running a merge.
- R07 against W14d's **D3** probe-count drift. Its diff adds the necessary key, but **nobody has executed that test**; its D3 outcome is **UNKNOWN**.
- `scripts/preflight.sh` — not run.
- `emc_fourth_cohort_quant.py --check` against the **live tree** — deliberately not run; scratch copy only, as recorded above.
- The Ensembl fetch and the vendor manifest — **not attempted**; they remain honest unrecovered sources.

---

## Limitations

- **This is an audit of code and file bytes. Nothing here is a clinical, biological or efficacy claim.** There is no wet lab. Runs are not samples and samples are not people.
- **I did not execute either repair.** My "R07/R08 move nothing" result is a computed confirmation of the *derivation* being reproducible (the unrepaired write path regenerates both TSVs bit-identically), plus a reading of two diffs that change only the comparison. It is one step weaker than W14c's and W14d's own executed T5/T6, which I did not re-run and do not restate as mine.
- **C3's artifact file sha256 is UNKNOWN and not computable.** `_generated_utc` is a wall-clock field written on every run, so no fixed post-C3 artifact hash exists. Only `probe_counts_sha256` (`6730b5a0…`) is a determinate computed value.
- **C3 as priced here is the label change only.** W20g never authored it; my relabelling was applied to a *copy of the TSV in memory*, not to the generator. A real C3 must also decide the disclosure field's exact text, which changes the JSON further.
- **My C3 relabelling reproduces the code's own classification, not an independent one.** It uses `probe_to_gene` / `probe_to_several_genes` from the committed inputs cache and the k=12 rule from `:718-724`. If those are wrong, my relabelled hash is wrong in the same way.
- **The 211,362 `not_offered` sequences are not established to be probes.** Only 1,645 were ever tested. Their identity remains **UNKNOWN** offline, exactly as W20f and W20g left it.
- **HEAD moved under me** (`98a0833f` → `c81236b9`). The target file's sha256 is unchanged and matches what W14d read, so no measurement here is affected; I verified rather than assumed that.
- Model identity is **SELF-REPORT, not independently verified**.

---

## Stop condition

**Stated up front; MET.** The table in §5 is complete with recomputed hashes for all three candidates, the either/or is resolved with a mechanism (real and hard between R07 and R08; not an either/or with C3), and I ruled on nothing. Returned immediately on meeting it.

---

## Tool-call and wall-clock count actually used

**14 tool calls** (all `Bash`; one produced a persisted large-output file I did not re-read). **Wall clock 3 min 18 s measured** (`03:13:03Z` → `03:16:21Z`), plus report drafting. Well inside the ~40-call / ~40-minute target.

---

## Next concrete action

**One, and it is small, executable offline, and settles the only remaining capability question between R07 and R08:** run W14d's **D3** negative control (probe TSV row 2, first count `0` → `1000000`, sha `e7b31a6e…`) against **R07's patched script** on a scratch copy, and record the exit code and whether the corruption survives. R07's diff adds `probe_counts_sha256` to the drift key tuple and so should exit 1 — but that has never been executed, and it is the one axis on which W24's flag currently credits R08 with a capability R07 may equally have. If R07 passes D3, the choice collapses to size, test count and memory cost, all of which are already tabulated above and all of which an owner can rule on in one reading. **That worker must still not land either repair.**
