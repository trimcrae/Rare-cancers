> Collected by the campaign coordinator from this worker's in-response draft.
> Per-child runtime model evidence from the child transcript: `"model":"claude-opus-5"` (OBSERVED).

---

Stop condition met. Report follows.

---

## Worker

- **Worker:** W09e, lane 9 refill, OPUS-CAPACITY-CAMPAIGN-20260908.
- **Model identity: SELF-REPORT, NOT INDEPENDENTLY VERIFIED.** I report myself as Claude Opus 5 (`claude-opus-5`). `env` exposes no model variable; `AI_AGENT=claude-code_2-1-263_agent` and `CLAUDE_CODE_VERSION=2.1.42` are harness versions, not model identities. The coordinator should extract the runtime model from the transcript.
- **Write isolation: honoured.** Nothing written under `/home/user/Rare-cancers`; no git write operation of any kind; `scripts/preflight.sh` not run. All execution under `/tmp/claude-0/w09e/`. **No view was hand-edited and no graph file was edited in the repository.** The graph correction exists only in the scratch copy.
- **HEAD actually read: `3f5fc95d806765b8fddf4fbe1dc288c85869fa2d`**, identical at start and end. ⚠ Not the brief's frozen `92abbcb905…`, and not W09d's `7d081218`. The tree has advanced again. All line numbers below are pinned to `3f5fc95d`.
- `git status --porcelain` at start: **empty**. At end: **empty**.
- **Sources read:** the live cloud checkout at `/home/user/Rare-cancers` only. The frozen corpus at `/tmp/claude-0/frozen-corpus/extracted/` was not needed and was not read. No network.

`date -u` at start: `Tue Sep  8 02:46:20 UTC 2026`. At end: `Tue Sep  8 02:50:19 UTC 2026`.

Literal output of `env | grep -i -E 'claude|anthropic|model' | sed -E 's/(TOKEN|KEY|SECRET)[^=]*=.*/\1=<redacted>/I'` (exit 0). The five long proxy lines (`no_proxy`, `NO_PROXY`, `GLOBAL_AGENT_NO_PROXY`, `npm_config_noproxy`, `JAVA_TOOL_OPTIONS`) are elided as marked; nothing else removed:

```
CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1
CLAUDE_CODE_ACCOUNT_UUID=4c503081-e4c3-45a1-8002-1cb705f46605
CLAUDE_CODE_CHILD_SESSION=1
[no_proxy=… elided]
AI_AGENT=claude-code_2-1-263_agent
CLAUDE_CODE_USER_EMAIL=trimcrae@gmail.com
CLAUDE_CODE_SESSION_ID=8ecd0f49-96ba-5dcf-b11a-af5e48bdec71
CLAUDE_CODE_DEBUG=true
CLAUDE_PID=522
CLAUDE_AUTO_BACKGROUND_TASKS=true
CLAUDE_AFTER_LAST_COMPACT=true
CLAUDE_EFFORT=medium
CLAUDE_CODE_GZIP_REQUEST_BODIES=1
CLAUDE_CODE_PROVIDER_MANAGED_BY_HOST=1
CLAUDE_CODE_MESSAGING_SOCKET=/tmp/cc-socks/522.sock
CLAUDE_CODE_CONTAINER_ID=container_0166QEHnXrRA8nCR59c9UG4k--claude_code_remote--71b0d4
CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=80
CLAUDECODE=1
SESSION_INGRESS_URL=https://api.anthropic.com
CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default
CLAUDE_CODE_WORKER_EPOCH=1
CLAUDE_CODE_REMOTE_SESSION_ID=cse_01Eui7FVgatEXAwt2N35yHH6
CLAUDE_CODE_PROXY_RESOLVES_HOSTS=true
CLAUDE_CODE_DISABLE_TERMINAL_TITLE=1
[GLOBAL_AGENT_NO_PROXY=… elided]
ANTHROPIC_BASE_URL=https://api.anthropic.com
CLAUDE_CODE_DIAGNOSTICS_FILE=/tmp/claude-code-303934770.diag.log
CLAUDE_ENABLE_STREAM_WATCHDOG=1
CLAUDE_CODE_REMOTE_HERMETIC_MODE=0
CLAUDE_CODE_ENVIRONMENT_RUNNER_VERSION=release-ba76006550-ext
CLAUDE_CODE_DISABLE_BUILTIN_ANTMCP=1
CLAUDE_ADDITIONAL_DIRECTORIES=/mnt/user-data
[JAVA_TOOL_OPTIONS=… elided]
[NO_PROXY=… elided]
CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH=1
CLAUDE_CODE_USE_CCR_V2=true
CLAUDE_CODE_SYNC_SESSION_REFS=1
DOCUMENTS_MCP_SCRATCH_ROOT=/mnt/user-data/working/claude-docs
CLAUDE_CODE_TEE_SDK_STDOUT=true
CLAUDE_CODE_ENTRYPOINT=remote
CLAUDE_CODE_VERSION=2.1.42
CLAUDE_CODE_EXECPATH=/opt/claude-code/bin/claude
[npm_config_noproxy=… elided]
CLAUDE_CODE_REMOTE_SEND_KEEPALIVES=true
CLAUDE_CODE_REMOTE=true
CLAUDE_CODE_POST_FOR_SESSION_INGRESS_V2=true
CLAUDE_CODE_ORGANIZATION_UUID=9f10e945-e412-4b71-8a9d-5d6affcd7d4f
CLAUDE_SESSION_INGRESS_TOKEN=<redacted>
CLAUDE_CODE_SYNC_SKILLS=1
CLAUDE_CODE_HOLD_UNANSWERED_PARKED_PERMISSION=1
CLAUDE_CODE_MESSAGING_TOKEN=<redacted>
```

---

## Question

What is the complete, measured set of generated lines under `systems/views/` that derive from `systems/graph/publications.json:119` (`PUB-CARE-DELIVERY.why_not_written`); does a correction to the C5 clause inside it leave every other generated byte identical; is S18 item 2 the same defect; and what wording does committed evidence actually support?

It is open because W09d measured the blast radius of **one clause** with a sentinel (5 lines) but not the field, did not produce a real correction diff, and explicitly declined to settle the wording ("that is a distinct scientific judgement, and my dispatch is a propagation question").

**Answers, all established by regeneration: 16 committed view lines derive from line 119, of which 11 are non-blank and 5 are blank separators; the proposed clause correction changes exactly 5 lines in 5 files and nothing else in the entire tree moves; S18 item 2 is a second, disjoint instance of the same defect class, not the same defect; and committed evidence supports a positive correction, because Chiusole 2020 *does* report treatment setting.**

---

## Prior-work check

- Read in full, as dispatched: `COMMON-BRIEF.md`, `CLOSED-WORK.md`, `CORPUS-CONTEXT.md`, `W09d-views-regeneration-proof.md`, `systems/POLICY-evidence.md`.
- `grep -n "C3\|C4\|C5" W09b-absence-claim-audit.md` → lines 200–202 are the C3/C4/C5 rows. C5 names only two views and routes the fix as "upstream in `systems/graph/`" without naming a file. C4 says of `treatment_setting`: *"Wording only. The evidence beside it is honest; the key is the categorical part."* **Not re-litigated — I take W09b's classification as given and act on the wording it flagged.**
- W09d read in full. I am **not replaying** it: W09d measured the C5 *clause* sentinel (5 lines) and the `artifacts.json` null result. I measure the *whole field* (16 lines), produce a real correction diff W09d did not, and settle the wording W09d declined. W09d's numbers are reproduced independently below and **agree**.
- `grep -rln "PMC13005408\|extracorporeal" --exclude-dir=.git .` → PMID 41689087 exists in four `research/literature/*-probe.json` files as **metadata only** (title, journal, year, OA flag). No abstract and no full text of it is committed. The EMC-case detail lives only in `reports/W09-care-access-referral.md:154`.
- `CLOSED-WORK.md` read. I am not replaying PUB-EMC-CLASSIFICATION (user-rejected), the NR4A Perspective refusal, lane 11's source-index, any Brenca route, or any denied external route. **No network was used at all.**

---

## Method / inputs

| input | role |
|---|---|
| `/home/user/Rare-cancers` @ `3f5fc95d` | tree under test, read-only |
| `systems/systems_check.py` | the generator actually run |
| `systems/graph/publications.json:119` | the source field under test (`PUB-CARE-DELIVERY.why_not_written`, record starts at line 112) |
| `systems/graph/routes.json:8536` | S18 item 2's target |
| `systems/views/` (**111 files**) | the generated layer |
| `research/modalities/emc-surgical-quality.json` | the evidence for the C5 wording decision |
| `research/modalities/emc-care-delivery-evidence.json` | the evidence for S18 item 2 |
| `systems/POLICY-evidence.md` | read in full before concluding |
| `/tmp/claude-0/w09e/copy/` | scratch copy via `tar --exclude=./.git`; the only place anything was executed or modified |

Python 3 stdlib only. No network, no paid API, no GPU.

---

## Result

### R.1 — Generator and the three render paths that read line 119. `PRIMARY`

Generator: `systems/systems_check.py`. Command: `python3 systems/systems_check.py --write-views`, run inside the scratch copy. **Real exit code 0** on every run below; it prints `systems_check: wrote 111 view(s) to systems/views/`.

`grep -n "why_not_written" systems/systems_check.py` returns four code sites, three of which render:

| site | path | what it emits |
|---|---|---|
| `:3046-3047` | L2 route views | `**It is not written because:** {p['why_not_written']}` — **full field, unclipped** |
| `:3223-3225` | L3 endpoint table | `gap = p.get("why_not_written") or "—"` then `esc(_clip(gap, 150))` — **clipped to 150 chars** |
| `:3239-3240` | L3 per-endpoint detail | `**Not written because:** {p['why_not_written']}` — **full field, unclipped** |
| `:609` | comment only | not a render |

⭐ **The `_clip(gap, 150)` table path at `:3223` is a fourth reader W09d did not report.** It matters: it is why the field-level blast radius is 6 rendered occurrences but the *clause*-level correction touches only 5 — the C5 clause sits well past character 150 and is clipped away in the table row.

### R.2 — THE ANSWER TO PART 1. Complete measured set of committed view lines deriving from `publications.json:119`. `PRIMARY`

Measured by replacing the **entire** field value with `W09E-FIELD-SENTINEL` and regenerating. **5 files differ; 6 sentinel occurrences; 16 committed lines are consumed.** The field contains a literal `\n\n`, so it renders as three markdown lines (paragraph, blank, paragraph) at each unclipped site.

| view file | committed lines derived | shape |
|---|---|---|
| `systems/views/L2-rt-surgical-quality.md` | **93, 94, 95** | prose / blank / prose |
| `systems/views/L2-rt-surveillance.md` | **92, 93, 94** | prose / blank / prose |
| `systems/views/L2-rt-metastasectomy.md` | **92, 93, 94** | prose / blank / prose |
| `systems/views/L2-rt-risk-model.md` | **93, 94, 95** | prose / blank / prose |
| `systems/views/L3-publications.md` | **69** | endpoint-table row, clipped at 150 chars |
| `systems/views/L3-publications.md` | **507, 508, 509** | prose / blank / prose |

**Total: 16 committed lines in 5 files — 11 non-blank, 5 blank separators.** W09d's 5-line figure was the clause radius and is a strict subset (the second prose line of each block, plus nothing in the table row).

### R.3 — THE ANSWER TO PART 2. Correction diff: 5 files, 5 lines, nothing else moved. `PRIMARY`

Applying the proposed clause replacement (R.6) to a pristine scratch copy and regenerating:

- **Changed files: 5.** Byte-verified by md5 over all 111 views (`diff a.md5 b.md5` → exactly 5 `<`/`>` pairs; the other **106 views are byte-identical**).
- **Changed lines: 5 old → 5 new**, one per file, **at identical line numbers on both sides** (no line-count shift, so no downstream renumbering):

| view file | changed line (old = new) |
|---|---|
| `L2-rt-surgical-quality.md` | 95 |
| `L2-rt-surveillance.md` | 94 |
| `L2-rt-metastasectomy.md` | 94 |
| `L2-rt-risk-model.md` | 95 |
| `L3-publications.md` | 509 |

- **`L3-publications.md:69` (the endpoint-table row) does NOT change**, because `_clip(gap, 150)` cuts before the edited clause. Correct behaviour, and worth stating explicitly so nobody reads its unchanged text as a missed copy.
- **Whole-tree byte comparison:** `diff -rq /home/user/Rare-cancers /tmp/claude-0/w09e/copy --exclude=.git` after the edit lists **exactly 6 differing files** — `systems/graph/publications.json` plus the 5 views. **Nothing else in the repository moved.** Parsed-JSON comparison confirms only record `PUB-CARE-DELIVERY`, key `why_not_written`, changed; 33 records preserved.

⚠ **One consequence the lander must know.** Because the correction uses the repository's rule-1.2 retention convention, the literal string *"treatment setting is reported by no reachable series"* **still appears once in each of the 5 views**, now inside `⚠ Superseded, retained (rule 1.2): “…”`. A grep-based acceptance test that merely counts the sentence will still fire. The test must assert the sentence is **quoted-and-superseded**, not **absent**.

### R.4 — THE ANSWER TO PART 3. S18 item 2 is a second, disjoint instance — not the same defect. `PRIMARY`

Live at this HEAD, verified structurally (`json.load` → `RT-METASTASECTOMY.supporting_evidence[0]`, exactly one matching route) and textually at `systems/graph/routes.json:8536`:

```
"what_it_supports": "the measured absence of any EMC metastasectomy record in a 554-record open-access corpus"
```

Independent sentinel + regeneration (`GEN_EXIT=0`) → **exactly 1 file, 1 line**:

```
copy/systems/views/L2-rt-metastasectomy.md:50:| `ART-CARE-DELIVERY-EVIDENCE` | W09E-ROUTES-SENTINEL | `direct` |
```

| axis | C5 (`publications.json:119`) | S18 item 2 (`routes.json:8536`) | same? |
|---|---|---|---|
| source file / field | `publications.json`, `PUB-CARE-DELIVERY.why_not_written` | `routes.json`, `RT-METASTASECTOMY.supporting_evidence[0].what_it_supports` | **no** |
| subject of the absence | treatment setting / referral | EMC metastasectomy records in a corpus | **no** |
| view lines reached | 16 (5 by the clause), 5 files | 1, 1 file | **no** |
| overlap | `L2-rt-metastasectomy.md:94` | `L2-rt-metastasectomy.md:50` | **disjoint** |
| epistemic status | mis-scoped and mis-named; a positive counter-record exists in the tree | **REFUTED** by a pinned full-text quote already committed | **no** |
| defect class | categorical absence asserted in graph source, propagating to generated views | identical class | **yes** |

**Verdict: a second instance of the same defect class, at a different source, with a disjoint blast radius. Not the same defect.** They can be landed independently; neither fixes the other.

Item 2's replacement re-verified against the evidence independently of W09d, in `research/modalities/emc-care-delivery-evidence.json`: the absence row `no-emc-metastasectomy-literature` is already `"status": "REFUTED"` (line 100); the quote at line 115 reads *"Eight patients (27.6%) underwent metastasectomy, including six, one, and one who underwent lung, bone, and lymph node resections, respectively."* with `denominator: 29`; line 118 records that **no outcome, survival, recurrence or comparator attaches to those eight**. **S18 item 2's replacement text remains accurate at this HEAD.** ⚠ The row itself warns the corpus was never re-swept, so *how many* records match is **UNKNOWN**; 1 is a measured lower bound.

⚠ **The regeneration step is mandatory and is absent from S18's instruction**, which says only "replace". Without `python3 systems/systems_check.py --write-views` in the same change, `L2-rt-metastasectomy.md:50` keeps the refuted wording. (Independently confirmed here; W09d found the same.)

### R.5 — Why the C5 clause is wrong, from committed evidence. `PRIMARY`

`research/modalities/emc-surgical-quality.json`, read structurally:

| field | value | bearing |
|---|---|---|
| `/series/0` (masunaga2025) `treatment_setting_printed` | `null` | setting genuinely not reported |
| `/series/0/⛔_setting_absent` | *"No centre, centre volume or referral status is printed for any patient. A national registry could in principle carry it; this publication does not report it."* | **supports** "not reported" for this series |
| `/series/1` (chiusole2020) `setting` | *"Istituto Oncologico Veneto and Institut Gustave Roussy — two referral centres, 1980-2018"* | ⭐ **the setting IS reported** |
| `/series/1/⛔_setting_absent` | *"Every patient in this series was treated at one of two sarcoma referral centres, so the exposure the route asks about is held CONSTANT. That is not a missing field; it is a cohort that cannot answer the question by construction…"* | ⭐ **the file itself says this is not a reporting gap** |
| `/treatment_setting/⛔_this_is_a_reading_not_a_gap` | *"One does not report the exposure and the other holds it constant, so neither can answer the referral question and further curation of them will not change that."* | the correct, non-categorical form — already written, in the source file |

**Two independent defects in the clause:**

1. **It is factually wrong about Chiusole.** Treatment setting *is* reported there; it is uniform, hence uninformative as an exposure. "Reported by no reachable series" is false as written, and the evidence file it summarises says so in its own words.
2. ⭐ **It names the wrong field for the claim it justifies.** The clause it supports is *"whether the diagnosis was known before the operation"* — that is the **unplanned-excision** question, not treatment setting. Treatment setting is the *referral-centre* question, a different unknown (`emc-surgical-quality.json:9` calls it *"the route's SECOND unknown"*). This mismatch is not in W09b's C3/C4/C5 rows and is a fresh finding.

Bearing of `POLICY-evidence.md`: its `scope:` binds *"Clinical and epidemiological evidence only — the EMC registry, the manuscript's meta-analysis, and any pooled proportion or interval derived from published cohorts"*, owned by `research/data/emc-clinical-registry.json` and enforced by `scripts/validate-registry.mjs`. **`systems/graph/publications.json` is outside that scope** and this correction touches no registry datum, no pooled proportion and no interval. §2.6(h) — *"Selection bias is bounded by a census taken from the same denominator … the only honest bound on that difference is the size of the non-reporting set"* — is the clause the wording engages **by analogy**, and it is the reason the replacement states *which* series report and which do not rather than asserting a categorical zero.

### R.6 — THE ANSWER TO PART 4. The wording, and what backs it. `PRIMARY` / `SECONDARY` as marked

**Committed evidence does support a positive correction**, so no honest-negative fallback is needed. The proposed replacement for the clause at `publications.json:119` (exact text applied and diffed above; the rest of the 1,762-character field is byte-preserved):

> The third clause of what_it_would_claim, whether the diagnosis was known before the operation, is unstudiable in EMC from the reachable record — but not for the reason recorded here until 2026-09-08. ⚠ Superseded, retained (rule 1.2): “treatment setting is reported by no reachable series.” That was categorical where the evidence beside it is not, and it named the wrong field. Treatment setting IS reported by Chiusole 2020 — every patient was treated at one of two sarcoma referral centres, so the exposure is held CONSTANT rather than missing — while Masunaga 2025 prints no centre, centre volume or referral status for any of its 171 patients (research/modalities/emc-surgical-quality.json, both series' ⛔\_setting\_absent). Neither series can answer the referral question, and no further curation of them will change that. What is unstudiable is the pre-operative-diagnosis question itself: neither rate-bearing series records unplanned excision, and the one reachable open-access record of it in this histology is a single EMC case inside a three-case report (PMID 41689087, reported by W09 2026-09-08, not independently re-retrieved) with no denominator, so no rate follows.

Evidence backing each assertion:

| assertion | source | grade |
|---|---|---|
| Chiusole 2020 reports the setting (two referral centres) and holds it constant | `emc-surgical-quality.json` `/series/1/setting`, `/series/1/⛔_setting_absent` | **PRIMARY** (committed, read this run) |
| Masunaga 2025 prints no centre/volume/referral status, n=171 | `emc-surgical-quality.json` `/series/0/treatment_setting_printed: null`, `/series/0/⛔_setting_absent` | **PRIMARY** (committed) |
| further curation of either cannot answer the referral question | `emc-surgical-quality.json` `/treatment_setting/⛔_this_is_a_reading_not_a_gap` | **PRIMARY** (committed) |
| neither rate-bearing series records unplanned excision | `emc-surgical-quality.json` `/unplanned_excision/recorded_in_any_reachable_series: false` and `/unplanned_excision/candidate_proxies/⛔_why_not` | **PRIMARY** (committed) |
| PMID 41689087 records one EMC case with unplanned excision elsewhere and referral | `reports/W09-care-access-referral.md:154`, quoting the PubMed abstract | **SECONDARY, single-source, NOT independently re-retrieved** — the four `research/literature/*-probe.json` files hold **metadata only** for this PMID (title/journal/year/OA); no abstract or full text is committed. **UNKNOWN** whether the quoted sentence is verbatim. Hence the wording names its reporter and its non-retrieval in-line. |
| no rate follows from it | `W09-care-access-referral.md:252` and `:242` (*"build any rate from PMID 41689087"* is explicitly forbidden) | **SECONDARY**, and stated as a prohibition rather than a measurement |

⚠ **The graph's owner may reasonably prefer to drop the PMID 41689087 sentence entirely** and end the replacement at *"Neither series can answer the referral question, and no further curation of them will change that."* That version rests on **100% PRIMARY committed evidence** and loses nothing structural. I include the case-report sentence because it is what makes the categorical form indefensible, but it is the one clause in the replacement whose support is a worker report rather than a curated artifact, and it is separable in one edit.

⛔ **No clinical claim is made anywhere in this report.** Every figure quoted (171 registered patients; 59 patients across two centres; 8 of 29 metastasectomies; one case report) is a count of **what was reported or done**, with **no outcome, no comparator and no rate**. Nothing here states or implies efficacy, safety, tolerability, selectivity or appropriateness of any operation, referral pathway or modality in this or any disease. There is no wet lab and no clinical-readiness claim.

---

## Validation evidence

Environment for every run: scratch copy at `/tmp/claude-0/w09e/copy/`, from `tar --exclude=./.git` of `/home/user/Rare-cancers` @ `3f5fc95d` (`TAR_EXIT=0`, `PUBS_IDENTICAL` by `cmp`). Linux, Python 3 stdlib, no network. `find … -type f | wc -l` → **111 views** on both sides throughout.

### `RUN` — Control: regeneration with no edit is a no-op
```
$ cd /tmp/claude-0/w09e/copy && cp -r systems/views /tmp/claude-0/w09e/views-committed
$ python3 systems/systems_check.py --write-views
systems_check: wrote 111 view(s) to systems/views/
GEN_EXIT=0
$ diff -rq /tmp/claude-0/w09e/views-committed /tmp/claude-0/w09e/copy/systems/views
CONTROL_DIFF_EXIT=0
```
Idempotent at this HEAD. Every later diff is attributable to the edit alone.

### `RUN` — Whole-field sentinel: the complete derived set
```
occurrences of encoded value: 1     (guarded; script asserts ==1)
applied, len old = 1762
systems_check: wrote 111 view(s) to systems/views/   GEN_EXIT=0
DIFF_EXIT=1 — 5 files differ:
  L2-rt-metastasectomy.md  L2-rt-risk-model.md  L2-rt-surgical-quality.md
  L2-rt-surveillance.md    L3-publications.md
sentinel occurrences: 6  (L3-publications.md twice: table row + detail)
changed lines (unified diff, incl. blanks): 22  (16 removed / 6 added)
```

### `RUN` — Proposed correction applied, then regenerated
```
$ python3 /tmp/claude-0/w09e/apply_c5.py
occurrences of encoded clause: 1
re-parse OK, records = 33
records whose parsed content changed: ['PUB-CARE-DELIVERY']
changed keys within PUB-CARE-DELIVERY: ['why_not_written']
WRITTEN                                         APPLY_EXIT=0

$ cd copy && python3 systems/systems_check.py --write-views
systems_check: wrote 111 view(s) to systems/views/   GEN_EXIT=0

$ diff -rq views-committed copy/systems/views      FILES_DIFF_EXIT=1
  → 5 files
$ md5 over all 111 files each side: 5 differing, 106 byte-identical
$ diff -u -r … | grep -c '^[+-][^+-]'              10   (5 old + 5 new)
per-file changed line numbers (old | new):
  L2-rt-metastasectomy.md   94 | 94
  L2-rt-risk-model.md       95 | 95
  L2-rt-surgical-quality.md 95 | 95
  L2-rt-surveillance.md     94 | 94
  L3-publications.md       509 | 509

$ diff -rq /home/user/Rare-cancers /tmp/claude-0/w09e/copy --exclude=.git
  systems/graph/publications.json  +  the 5 views   → exactly 6 files, nothing else
```
⭐ **This is the deliverable: 5 files, 5 lines, no renumbering, and every other generated byte identical.**

### `RUN` — S18 item 2 sentinel (pristine copy restored first)
```
occ: 1 ; reparse OK
systems_check: wrote 111 view(s) to systems/views/   GEN_EXIT=0
DIFF_EXIT=1 — 1 file:  L2-rt-metastasectomy.md
copy/systems/views/L2-rt-metastasectomy.md:50:| `ART-CARE-DELIVERY-EVIDENCE` | W09E-ROUTES-SENTINEL | `direct` |
```

### `PROPOSED (NOT RUN)`
- **Acceptance test for the C5 correction.** Shape, not run: RED at HEAD asserting (a) `publications.json` `PUB-CARE-DELIVERY.why_not_written` contains the sentence **unquoted**, and (b) each of the 5 view lines contains it unquoted; GREEN after edit **plus** `--write-views`, asserting the sentence appears **only** inside `⚠ Superseded, retained (rule 1.2): “…”`. **A bare grep for the sentence will not work** (R.3).
- `scripts/preflight.sh` — dispatch forbids it, and no edit exists in the repository to gate.
- W09b's `lint_absence_claims.py` — not re-run. Its behaviour on a rule-1.2-retained string is **UNKNOWN**; see Limitations.
- Independent retrieval of PMID 41689087. **Blocked: no network authorised.** Not attempted, no route replayed.
- Landing anything. Nothing was applied to the repository.

---

## Limitations

- **Model identity is self-report**, not verifiable from this seat.
- **HEAD drift.** `92abbcb905…` (brief) → `103ff76f` (W09b) → `b9a0257e` (W09c) → `7d081218` (W09d) → `3f5fc95d` (mine). Every line number here is pinned to `3f5fc95d` and will drift.
- **The diff proves scope, not correctness.** It shows exactly what a correction moves. Whether the graph's owner accepts my wording is a judgement I am proposing, not measuring.
- **The PMID 41689087 sentence is SECONDARY and unverified.** Only metadata for it is committed; the EMC-case detail exists in one worker report and I could not re-retrieve it. The wording flags this in-line, and R.6 gives a fully-PRIMARY variant that drops it.
- **I did not re-audit W09b's C1/C2/C3/C4/C6 or its 35% precision measurement (7/20), and did not run its linter.** Those verdicts stand exactly as recorded. **UNKNOWN** whether the linter tolerates a rule-1.2 retention.
- **View count changed between seats** (W09d: "111 views", `ls` of the directory here: 107 entries but **111 files** — some views live in subdirectories). W09d's and my file-level results agree.
- **The scratch copy is left with `L2-rt-metastasectomy.md` still carrying the routes sentinel** (I restored `routes.json` but did not re-regenerate). Scratch only; the repository is untouched and `git status` is empty at start and end.
- **No network, no external source resolved, no clinical claim, no manuscript touched.** No manuscript, SI or deposit reads either corrected field (S18 verified this by grep; I did not re-verify it).

---

## Stop condition

**Set up front: MET, all four parts.**

1. **Complete derived set enumerated by regeneration, not grep** — 16 committed lines in 5 files (11 non-blank + 5 blank), with a fourth render path (`_clip`, `systems_check.py:3223`) that W09d did not report. Generator, command and real exit code (0) stated.
2. **Correction applied in scratch, full diff produced** — 5 files, 5 lines, identical line numbers, 106 of 111 views byte-identical by md5, and exactly 6 files differing across the whole tree. **Nothing else moved.**
3. **S18 item 2 adjudicated** — second, disjoint instance of the same defect class; 1 view line (`L2-rt-metastasectomy.md:50`), evidence re-verified independently, replacement still accurate, regeneration step mandatory and missing from S18's instruction.
4. **Wording stated with its evidence** — positive correction supported by PRIMARY committed evidence (Chiusole reports the setting), with the one SECONDARY sentence marked and separable, plus the fresh finding that the clause justified the wrong question.

Nothing was applied. Nothing was written into the repository.

---

## Tool-call and wall-clock count actually used

**20 tool calls** (all `Bash`). **Wall clock ≈ 4 minutes** — `02:46:20Z` start, `02:50:19Z` end. Well inside the ~40-call / ~40-minute target. Returned as soon as the stop condition was met; no padding.

---

## Next concrete action

**One task for the graph's owner (a decision, not a worker task):** accept or amend the R.6 wording — specifically, decide whether the PMID 41689087 sentence stays (SECONDARY, one worker report, not re-retrieved) or is cut for the fully-PRIMARY variant. Then land it as **one change containing three parts**: (a) the `publications.json:119` clause edit, (b) `python3 systems/systems_check.py --write-views`, which is mandatory and moves exactly the 5 lines listed in R.3, and (c) an acceptance test written to the R.3 shape — asserting the sentence appears **only quoted-and-superseded**, never asserting its absence, because rule 1.2 retention keeps the literal string present.

**Secondary, independent, cheap:** S18 item 2 can land separately and must carry the same `--write-views` step, or `L2-rt-metastasectomy.md:50` keeps refuted wording.

**One open item this lane could take next:** `systems/graph/publications.json` has now been shown to contain one false-absence copy and has **never been audited as a whole** — S18 explicitly did not touch `systems/graph/*.json`, and W09b/W09c/W09d examined only `artifacts.json` and `routes.json`. The other 32 `why_not_written` / `what_it_would_claim` fields in that file are **UNKNOWN**, and the sentinel-plus-regeneration method used here measures any of them in about two minutes each.
