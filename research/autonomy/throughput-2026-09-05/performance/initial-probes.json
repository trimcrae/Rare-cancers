{
  "initial_ablation_probe": "Stopped: default pytest tmp_path root was permission denied; rerun with PYTEST_DEBUG_TEMPROOT under task cache.",
  "metadata_ablation_probe": "Stopped after more than six minutes without completing the first sentence; raw diff showed ORCID digits being perturbed because front matter and the first abstract sentence share one census span. No result or speedup is inferred from this interrupted probe. Controlled timing uses evenly spaced interior samples to bound runtime; the production full sweep still includes all sentences.",
  "collector_initial_probe_log": "retro-collector-before.log",
  "collector_interrupted_verdict": "retro-collector-before-corrected.log records all five tests finishing but the tracked-tree guard rejected the session because preflight.sh was edited concurrently. Repeated on a settled tracked tree."
}
