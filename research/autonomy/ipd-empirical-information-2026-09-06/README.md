---
id: DOC-IPD-EMPIRICAL-README-20260906
title: Empirical information checkpoint
kind: memo
status: live
date: 2026-09-06
last_verified: 2026-09-06
purpose: Preserve source evidence, exact execution scope and limitations for the current research checkpoint.
scope: Reader metadata added at integration; scientific source body unchanged.
audience: [maintainers, autonomous research agents]
---

# Empirical-information development checkpoint

Completed result: all120 development cases returned; the prespecified continuation gate failed. See `result.md`. Eleven reserved source groups remain unopened and no manuscript or further expansion is selected. Metadata/protocol/harness were prepared before empirical outcome access. `prepare_metadata.py` reads source identifiers, headers and TCGA patient IDs only; CSV parsing reads record bytes but never accesses time/status values or subgroup outcome columns. The saved primary metadata and package sources are in the sibling `ipd-published-baselines-2026-09-06` packet.

`source-manifest.json` finds 21 TCGA patient-ID-disjoint components: first ten deterministic hash-ranked components are development; eleven remain reserved. GEO curves are quarantined. Same-source variants and seeds cannot cross the split. Absence of exact TCGA ID overlaps is not an independent clinical provenance audit. Source times have unspecified units; all proposed statistics compare pseudoarms within one source only.

`protocol.md` supplies the frozen selection, numerical release, original realized decision target, same-prediction matched-retention rules and a prospective continuation gate. No change to failed inverse-solver claims. No paper claim or held-out authorization.

Run default `pilot.py` using the existing bundled Python for synthetic arithmetic/release checks and current hashes. It does not open empirical outcomes. The independent formula fixtures cover exact tied-event U/V/Q, arm-swap sign, zero variance, event-before-censor risk release and common time-scale invariance. An initial test used exact floating p equality under arm swap and was corrected to 1e-12 tolerance; no empirical values had been read.

The completed command and all four approved protocol/manifest/amendment hashes are in `authorization.json`. `pilot.py --run` also requires `--amendment-sha256` and `--runtime-amendment-sha256`; do not overwrite preserved execution. `runner.R` retains both original package calls with identical released arrays and the documented exact-time interface wrapper. R and package paths point to the existing baseline worktree cache, read-only. Per-case returned rows, warnings/failures and independently checked statistics are retained under `development/`. `summarize.py` computes the frozen descriptive comparisons and gate. `verify_original.py` independently checks the30 original assignments in R, opening only development records.

Model/effort are inherited from this worker; token usage unavailable. No commit, push, manuscript, full preflight, publication, package installation, paid API or GPU use. Coordinator owns independent protocol review and any subsequent execution authorization.
