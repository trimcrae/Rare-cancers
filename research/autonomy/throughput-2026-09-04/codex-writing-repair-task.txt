Perform one focused repair of the already committed ASO submission support package. Own ONLY research/release-candidates/PUB-ASO/2026-09-04/submission/README.md and upload-manifest.json. The previous task produced the package; its failed receipt and original outcome are preserved under research/autonomy/throughput-2026-09-04/codex-writing-initial-*.json. Do not rewrite those records or the cover letter, manuscript, canonical sources, or scientific claims.

Make two corrections:
1. The README currently makes author acceptance a prerequisite to generating draft Word/anonymous upload variants. Remove that unnecessary early approval gate. Freeze the current candidate bytes and prepare/check explicitly unsubmitted draft variants first. Author review, declarations, fee decisions and explicit journal-submission approval belong at the final concrete upload-package decision; unresolved declarations can stay visibly marked while drafting proceeds. Keep the README under 700 words and preserve its useful archive and source-version caveats. Do not generate Word/PDF files in this repair.
2. The manifest's execution metadata guessed 20 minutes, a generic model and unknown effort. Replace those guesses with this repair's actual configured values from the runner-supplied execution context, clearly labeled as configuration rather than observed duration. Set the base revision to the actual checkout and note the two working-tree edits. Recompute the hash of the changed README and verify all 25 existing-file hashes and paths. Make the next action generation/verification of the unsubmitted draft upload variants; approval is not needed to prepare them. Preserve missing-variant nulls and all scientific/provenance facts.

Acceptance: the two corrections are made; JSON parses; all inventoried paths and raw SHA256 hashes match; the README remains under 700 words with required front matter; no other tracked or untracked project paths changed. At most four read/search commands plus the edit and one focused verification command. No full preflight or science suites, external operations, new review round, or new paper. Return completed with blockers empty if this repair meets those criteria. List later upload/author/archive work in follow_up; never claim submission or publication readiness.