---
id: DOC-TRIAL-DIFFERENCE-PACKET-20260906
title: Exact trial difference preparation checkpoint
kind: report
status: preparation-only-hold-for-methods-and-value-decision
purpose: Deliver a source-only finite comparison without authorizing new relevance judgments.
scope: EMC archived hierarchy versus augmented top-100 at K=100.
audience: [coordinator, methods reviewers]
date: 2026-09-06
last_verified: 2026-09-06
---

The archived rankings identify 83 common records, 17 entrants and 17 displaced records.
Neither cutoff has a tie crossing 100. Under one identical binary disease-scope rule on
identical record versions, common contributions cancel. The 34-record census can therefore
identify a finite net relevant-record yield difference, with conservative bounds for unresolved
records. It cannot identify absolute precision or recall, clinician time, eligibility or benefit.

**Recommendation: HOLD after this preparation checkpoint.** Return protocol.md and
coordinator/packet-manifest.json for independent methods review. No reader session is running
or authorized by this packet. The original screen remains FAILED (17<20, although 13>=10).
Clean provenance and algebraic identifiability do not demonstrate relevance gains, novelty,
publishability or that trial retrieval outranks the therapeutic research prospects.

A later value decision would need to justify a narrow, useful contribution: whether molecular
augmentation adds disease-scope-compatible records at a fixed review budget over the existing
hierarchy baseline in this particular saved EMC corpus. It must accept that the answer could
be negative or remain directionally uncertain, and that this alone would be a small snapshot
audit rather than a general retrieval benchmark. Do not fund broader labeling merely because
this packet can be built. There is no new outcome evidence here to revise the portfolio rank.

Files:

- protocol.md: prospective rule, source/version policy, taxonomy, bounds and reading freeze.
- build_packet.py: standard-library, offline, allowlisted ZIP-member builder and verifier.
- test_semantics.py: nine synthetic behavioral tests; no empirical labels.
- reader/: only a source manifest, generic protocol and 34 complete raw registry records.
- coordinator/packet-manifest.json: exact groups, ranks, source byte slices, versions, hashes,
  complete input read inventory and deterministic ordering seed; keep from future readers.
- integrity-receipt.json: frozen file inventory, protocol and packet hashes, execution scope.

Run from the repository with a working Python 3 executable:

```text
python -B -X utf8 research/autonomy/trial-difference-design-2026-09-06/test_semantics.py
python -B -X utf8 research/autonomy/trial-difference-design-2026-09-06/build_packet.py build
python -B -X utf8 research/autonomy/trial-difference-design-2026-09-06/build_packet.py verify
```

Build refuses changed existing outputs. Verify checks the frozen inventory and reconstructs
all generated bytes from the pinned original package without writing or reranking. Do not
delete a freeze to alter this experiment: append a dated amendment and prepare a newly identified
packet if independent review finds a substantive defect. The archive remains at its original
package path; it is not duplicated. The source packet contains no historical relevance labels.

The nine semantic tests were run successfully before freezing. Full source checks are recorded
only when the builder succeeds; the coordinator should retain the actual verify command result.
No repository-wide preflight, old pilot, score run, network retrieval or publication check is
claimed. This isolated preparation has no commit; integration and normal settled-tree preflight
belong to the coordinator. Scientific seconds and subscription usage are unavailable. No process
is left running. Outputs must remain stopped at this checkpoint until separate authorization.
